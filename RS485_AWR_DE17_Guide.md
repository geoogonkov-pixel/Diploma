# Моделирование линии RS-485 на витой паре в AWR Design Environment 17

## Полная практическая инструкция

---

## 1. Выбор модели: что именно строим

### 1.1. Что моделируется физически

Моделируется **полный тракт передачи дифференциального сигнала RS-485**: от выхода драйвера (передатчика) через витую пару заданной длины до входа приёмника с терминирующим резистором. Модель включает:

- **Драйвер RS-485** — эквивалентная схема: два противофазных источника напряжения с выходным сопротивлением. Моделируется двумя портами (PORT) с последовательными резисторами (RES), задающими выходной импеданс драйвера. Это не транзисторная модель конкретной микросхемы, а её **эквивалент Тевенина** — стандартный и корректный подход.
- **Витая пара (кабель)** — моделируется элементом **CLIN** (Coupled Ideal Transmission Lines) — связанные идеальные линии передачи. CLIN — это **4-портовый элемент**, представляющий два связанных проводника с заданными импедансами чётной и нечётной мод. Это корректное представление витой пары: два проводника электромагнитно связаны, сигнал распространяется в дифференциальном (нечётная мода) и синфазном (чётная мода) режимах одновременно.
- **Терминирующий резистор** — явный элемент RES (120 Ом), подключённый **между линиями A и B** на приёмном конце. Это физически точная модель: в реальной системе RS-485 терминатор устанавливается между проводами витой пары.
- **Приёмник RS-485** — моделируется высокоомными резисторами (RES, 12 кОм каждый) от линий A и B к земле. Это соответствует входному сопротивлению приёмника RS-485 по стандарту EIA/TIA-485.
- **Паразитные параметры** — входная ёмкость приёмника (CAP, ~15 пФ на каждый вход), которая влияет на высокочастотное поведение и фронты сигнала.
- **Земля** — общий опорный провод (GND), к которому привязаны все потенциалы.

### 1.2. Почему именно CLIN, а не TLIN

| Подход | Что моделирует | Ограничения |
|--------|----------------|-------------|
| **TLIN** (одиночная линия) | Только дифференциальный режим, один эквивалентный провод | Нет синфазного режима, нет двух линий A и B, невозможно показать терминатор между линиями, слишком упрощённо |
| **CLIN** (связанные линии) | Оба проводника витой пары с электромагнитной связью | Идеальная модель без потерь, но их можно добавить |
| **MCLIN** (связанные микрополоски) | Дифференциальная пара на печатной плате | Для PCB, не для кабеля; требует подложку MSUB |

**CLIN выбран потому что:**
- Корректно представляет **два физических проводника** витой пары.
- Позволяет задать **дифференциальный импеданс** через импедансы мод (Z0E, Z0O).
- Даёт **4-портовую S-матрицу**, из которой можно получить смешанные (mixed-mode) S-параметры: Sdd (дифференциальный), Scc (синфазный), Sdc/Scd (конверсия мод).
- Терминирующий резистор подключается **между линиями** — как в реальности.
- Модель не слишком сложна, но при этом физически адекватна.

### 1.3. Допустимые упрощения

| Что упрощено | Как упрощено | Почему допустимо |
|--------------|--------------|------------------|
| Драйвер | Эквивалент Тевенина (PORT + RES) вместо транзисторной модели | Для анализа линии передачи важен только выходной импеданс, а не внутренняя структура |
| Потери в кабеле | Не учтены (CLIN — без потерь) | Для кабеля 10–50 м на частотах до 50 МГц потери малы (< 1 дБ). Можно добавить позже |
| Скрутка витой пары | Не моделируется геометрически | CLIN задаёт связь между проводниками через импедансы мод — это эквивалент физической скрутки |
| Приёмник | Резистор + конденсатор вместо полной модели | Входной каскад приёмника — это высокоомный вход с паразитной ёмкостью; для анализа линии этого достаточно |
| Множество приёмников на шине | Один приёмник | Упрощение для базовой модели; можно добавить ещё приёмники позже |

### 1.4. Соответствие элементов модели и реальной системы RS-485

| Реальный компонент RS-485 | Элемент модели AWR | Что моделирует |
|---------------------------|---------------------|----------------|
| Микросхема драйвера (SN75176, MAX485 и т.д.) | PORT1 + RES(R_drv_A), PORT2 + RES(R_drv_B) | Два противофазных выхода с выходным сопротивлением |
| Кабель витой пары (Belden 9841, КПСВЭВ и т.д.) | CLIN | Два связанных проводника с дифференциальным Z₀ = 120 Ом |
| Терминирующий резистор 120 Ом | RES(R_term) | Резистор между линиями A и B |
| Входы приёмника (микросхема) | RES(R_rcv_A), RES(R_rcv_B) + CAP(C_rcv_A), CAP(C_rcv_B) | Высокоомный вход с паразитной ёмкостью |
| Экран кабеля / заземление | GND | Общая точка потенциала |
| Паразитные эффекты | CAP на входе приёмника | Входная ёмкость приёмника (~15 пФ) |

---

## 2. Обязательные элементы модели

### 2.1. Полный список элементов

| № | Элемент AWR | Кол-во | Назначение | Почему обязателен | Основные параметры |
|---|-------------|--------|------------|-------------------|--------------------|
| 1 | **PORT** | 2 шт. | Дифференциальный источник (драйвер RS-485, линии A+ и B−) | Без источника нет сигнала. Два порта задают дифференциальный вход | PORT1: Z = 50 Ом; PORT2: Z = 50 Ом |
| 2 | **RES** (R_drv_A) | 1 шт. | Выходное сопротивление драйвера, линия A | Без него источник подключён напрямую к линии — нереалистично; драйвер RS-485 имеет конечный выходной импеданс | R = 50 Ом |
| 3 | **RES** (R_drv_B) | 1 шт. | Выходное сопротивление драйвера, линия B | Аналогично, для второго проводника | R = 50 Ом |
| 4 | **CLIN** | 1 шт. | Витая пара — связанная линия передачи | Это **основной моделируемый объект**. Без него нет линии. CLIN корректно описывает два связанных проводника | Z0E = 90 Ом, Z0O = 60 Ом, EL = 60°, F0 = 0.005 ГГц |
| 5 | **RES** (R_term) | 1 шт. | Терминирующий резистор между A и B | Без терминатора линия рассогласована — модель не покажет штатный режим RS-485 | R = 120 Ом |
| 6 | **RES** (R_rcv_A) | 1 шт. | Входное сопротивление приёмника, линия A | Без модели приёмника мы не знаем, какой сигнал приходит на вход микросхемы | R = 12000 Ом |
| 7 | **RES** (R_rcv_B) | 1 шт. | Входное сопротивление приёмника, линия B | Аналогично, для второго проводника | R = 12000 Ом |
| 8 | **CAP** (C_rcv_A) | 1 шт. | Входная ёмкость приёмника, линия A | Без неё модель не учитывает паразитную ёмкость, влияющую на высокие частоты и крутизну фронтов | C = 15 пФ |
| 9 | **CAP** (C_rcv_B) | 1 шт. | Входная ёмкость приёмника, линия B | Аналогично | C = 15 пФ |
| 10 | **GND** | 5–7 шт. | Земля (опорный потенциал) | Без земли схема не имеет общей точки, нет замкнутого контура тока | — |

### 2.2. Детальный разбор каждого элемента

#### Дифференциальный источник (PORT1 + PORT2)

В AWR DE 17 **нельзя напрямую задать один дифференциальный источник** в S-параметрическом анализе. Вместо этого используются **два одиночных порта** (PORT1 и PORT2), которые затем объединяются в **балансный (дифференциальный) порт** на этапе настройки измерений (Mixed-Mode S-Parameters). Это стандартный подход AWR для дифференциальных цепей.

- PORT1 подключён через R_drv_A к линии A (положительный вход).
- PORT2 подключён через R_drv_B к линии B (отрицательный вход).
- Каждый порт имеет Z = 50 Ом (стандартный импеданс порта).
- Реальное выходное сопротивление драйвера моделируется резисторами R_drv.

#### Связанная линия передачи (CLIN)

Путь в AWR: **Circuit Elements → Transmission Lines → Coupled Lines → CLIN**.

CLIN имеет **4 сигнальных вывода** и **земляные выводы**:
- Выводы 1, 3 — вход и выход **линии 1** (проводник A).
- Выводы 2, 4 — вход и выход **линии 2** (проводник B).

Параметры CLIN и их физический смысл:

| Параметр | Значение | Физический смысл | Как определяет витую пару |
|----------|----------|------------------|---------------------------|
| **Z0E** (Even-mode impedance) | 90 Ом | Импеданс чётной моды (синфазный сигнал) | Определяет поведение при синфазной помехе |
| **Z0O** (Odd-mode impedance) | 60 Ом | Импеданс нечётной моды (дифференциальный сигнал) | Определяет Zdiff = 2 × Z0O = 120 Ом |
| **EL** (Electrical Length) | 60° | Электрическая длина при опорной частоте | Определяет физическую длину кабеля |
| **F0** (Reference Frequency) | 0.005 ГГц | Опорная частота (5 МГц) | Частота, при которой EL соответствует физической длине |

**Связь параметров CLIN с характеристиками витой пары:**
- Zdiff = 2 × Z0O = 2 × 60 = **120 Ом** — дифференциальный импеданс (стандарт RS-485).
- Zcomm = Z0E / 2 = 90 / 2 = **45 Ом** — синфазный импеданс.
- Коэффициент связи: k = (Z0E − Z0O) / (Z0E + Z0O) = (90 − 60) / (90 + 60) = **0.2** — характеризует электромагнитную связь между проводниками витой пары. Для реальной витой пары k ≈ 0.1–0.3.

#### Волновое сопротивление линии

Задаётся **не напрямую**, а через Z0E и Z0O. Дифференциальный импеданс Zdiff = 2 × Z0O = 120 Ом — это основной параметр, который должен совпадать с номиналом терминирующего резистора.

#### Длина линии

Задаётся через электрическую длину **EL** при частоте **F0**. Формула пересчёта:

```
L_физ = EL × c / (360 × F0 × √εeff)
```

Где:
- L_физ — физическая длина (м)
- EL — электрическая длина (°)
- c = 3 × 10⁸ м/с
- F0 — опорная частота (Гц)
- εeff — эффективная диэлектрическая проницаемость среды (для витой пары в полиэтиленовой изоляции εeff ≈ 2.3, √εeff ≈ 1.52)

Примеры:

| EL (°) | F0 (ГГц) | L_физ (м) | Комментарий |
|--------|-----------|-----------|-------------|
| 60 | 0.005 | ≈ 6.6 м | Короткая линия |
| 90 | 0.005 | ≈ 9.9 м | Типичная (~10 м) |
| 180 | 0.005 | ≈ 19.7 м | Средняя (~20 м) |
| 36 | 0.005 | ≈ 3.3 м | Для стенда / лаборатории |
| 900 | 0.005 | ≈ 98.7 м | Длинная (~100 м) |

#### Задержка распространения

Не задаётся отдельно — вычисляется из EL и F0:

```
τ = EL / (360 × F0)
```

Для EL = 60° при F0 = 5 МГц: τ = 60 / (360 × 5×10⁶) = **33.3 нс** (что соответствует ~6.6 м кабеля при скорости распространения ~0.66c).

#### Потери в линии

CLIN — **идеальный (без потерь)** элемент. Для учебной модели это допустимо. Если нужно учесть потери, есть два способа:
1. Добавить небольшие последовательные резисторы (RES, ~1–5 Ом) на входе и выходе каждой линии CLIN для имитации омических потерь.
2. Использовать элемент **TLINP** (Lossy Transmission Line) вместо CLIN, но тогда теряется связь между линиями и модель становится однопроводной.

Для базовой модели потерями пренебрегаем — на длинах до 50 м при частотах до 50 МГц потери в типичном кабеле RS-485 составляют < 2 дБ.

#### Терминирующий резистор (R_term)

Путь в AWR: **Circuit Elements → Lumped Elements → Resistor → RES**.

- R = 120 Ом — подключается **между линией A и линией B** на приёмном конце.
- Без терминатора коэффициент отражения Γ ≈ 0.98 (практически полное отражение).
- С терминатором Γ → 0 (отражений нет).

#### Резисторы смещения (Bias Resistors)

В реальных системах RS-485 иногда устанавливают резисторы смещения (failsafe bias): ~390 Ом от линии A к +5В и ~390 Ом от линии B к GND. Они задают определённый уровень на линии при отсутствии передачи.

**Для базовой модели они не обязательны.** Их можно добавить позже для расширенного анализа, но для демонстрации работы линии они не критичны.

#### Приёмная нагрузка (R_rcv + C_rcv)

- R_rcv = 12 кОм на каждый вход (стандарт RS-485: входное сопротивление ≥ 12 кОм).
- C_rcv = 15 пФ на каждый вход (типичная входная ёмкость приёмника, например MAX485).
- R_rcv подключается от линии A (или B) к GND.
- C_rcv подключается параллельно R_rcv.

#### Порты, пробники, графики

- **PORT1, PORT2** — входные порты (сторона драйвера). Номера: 1, 2.
- **PORT3, PORT4** — выходные порты (сторона приёмника, опционально). Если используются, то номера: 3, 4. Подключаются после терминатора для измерения сигнала на входе приёмника.
- Порты 3 и 4 **не обязательны** для базовой модели — можно обойтись двухпортовой схемой (или 4-портовой с CLIN), а сигнал на приёмнике наблюдать через напряжение на R_term.
- **Графики** — создаются в разделе Graphs дерева проекта.

---

## 3. Пошаговая инструкция по созданию схемы

> **Предупреждение:** точные названия пунктов меню могут незначительно отличаться в зависимости от конкретной сборки AWR DE 17 (Cadence AWR Design Environment 17 vs. NI AWR Design Environment 16.x). Логика и последовательность действий при этом сохраняются. Ниже приведены наиболее распространённые названия.

### Шаг 1. Создание проекта

1. Запустите **AWR Design Environment** (Cadence AWR Design Environment 17).
2. Главное меню → **File → New Project** (или **Ctrl+N**).
3. В левой панели (Project Browser) появится пустое дерево проекта.
4. Сохраните проект: **File → Save Project As...** → имя: `RS485_TwistedPair` → **Save**.

**Результат:** пустой проект открыт, дерево проекта видно в левой панели.

### Шаг 2. Создание схемы

1. В дереве проекта найдите раздел **Circuit Schematics**.
2. Правой кнопкой мыши → **New Schematic**.
3. Имя схемы: `RS485_Diff_Line`.
4. Нажмите **OK** (**Create**).

**Результат:** откроется пустой лист схемы (Schematic Editor).

### Шаг 3. Размещение элемента CLIN (витая пара)

CLIN — центральный элемент схемы. Размещаем его первым, чтобы строить остальную цепь вокруг него.

1. В панели элементов (Elements Browser, обычно слева внизу):
   - Раскройте: **Circuit Elements → Transmission Lines → Coupled Lines**.
   - Найдите **CLIN** (Coupled Ideal Transmission Lines).
   - Альтернативный путь: меню **Draw → Add Element...** → в поле поиска введите `CLIN`.
2. Перетащите CLIN на центр листа схемы (или дважды щёлкните по CLIN, затем кликните на схеме).
3. Элемент CLIN должен разместиться горизонтально. Он имеет:
   - **Слева вверху:** вывод 1 (вход линии A)
   - **Слева внизу:** вывод 2 (вход линии B)
   - **Справа вверху:** вывод 3 (выход линии A)
   - **Справа внизу:** вывод 4 (выход линии B)
   - **Два нижних вывода** (снизу от основного символа): земля линии 1 и линии 2
4. Дважды щёлкните по размещённому CLIN → откроется окно **Element Options**.
5. Задайте параметры:
   - **ZE** (или Z0E, Even-mode impedance): `90` Ом
   - **ZO** (или Z0O, Odd-mode impedance): `60` Ом
   - **EL** (Electrical Length): `60` градусов
   - **F0** (Reference Frequency): `0.005` ГГц
6. Нажмите **OK**.

**Зачем эти значения:**
- Z0E = 90 Ом и Z0O = 60 Ом → Zdiff = 2 × 60 = **120 Ом** (стандарт RS-485).
- EL = 60° при F0 = 5 МГц → физическая длина ≈ 6.6 м (короткий кабель, удобный для наблюдения эффектов).
- F0 = 5 МГц — основная частота RS-485 при скорости 10 Мбит/с.

**Результат:** на схеме размещён элемент CLIN с четырьмя сигнальными выводами.

### Шаг 4. Подключение земли к CLIN

1. Найдите элемент **GND** (Ground):
   - Путь: **Circuit Elements → Ports → GND** (или используйте кнопку Ground на панели инструментов, или **Draw → Add Element → GND**).
2. Разместите **два элемента GND** — по одному под каждым земляным выводом CLIN (нижние выводы).
3. Инструментом провода (**Draw → Wire** или клавиша **W**) соедините:
   - Левый нижний земляной вывод CLIN → GND.
   - Правый нижний земляной вывод CLIN → GND.

**Зачем:** без заземления земляных выводов CLIN схема некорректна — возникнет ошибка «floating node» или неверные результаты. Земля CLIN — это экран кабеля / общий провод.

**Результат:** CLIN заземлён снизу, ошибок подключения нет.

### Шаг 5. Добавление резисторов драйвера (R_drv_A, R_drv_B)

1. Найдите элемент **RES** (Resistor):
   - Путь: **Circuit Elements → Lumped Elements → Resistor → RES**.
   - Или: **Draw → Add Element → RES**.
2. Разместите первый RES **горизонтально** слева от верхнего левого вывода CLIN (вывод 1).
   - При размещении, если элемент ориентирован вертикально, нажмите **Ctrl+R** для поворота до горизонтального положения.
3. Дважды щёлкните по RES → задайте:
   - **R:** `50` Ом
   - **ID:** `R_drv_A` (для удобства идентификации; если поле ID недоступно, оставьте по умолчанию)
4. Нажмите **OK**.
5. Аналогично разместите **второй RES** слева от нижнего левого вывода CLIN (вывод 2):
   - **R:** `50` Ом
   - **ID:** `R_drv_B`
6. Проводами соедините:
   - Правый вывод R_drv_A → вывод 1 CLIN (вход линии A).
   - Правый вывод R_drv_B → вывод 2 CLIN (вход линии B).

**Зачем:** R_drv моделирует выходное сопротивление драйвера RS-485. Типичное значение для драйверов типа MAX485, SN75176 — 30–60 Ом. Мы выбираем 50 Ом как стартовое значение. Суммарный дифференциальный выходной импеданс: 2 × 50 = 100 Ом.

**Результат:** слева от CLIN два горизонтальных резистора, подключённых к выводам 1 и 2.

### Шаг 6. Добавление портов (источников) — PORT1 и PORT2

1. Найдите элемент **PORT**:
   - Путь: **Circuit Elements → Ports → PORT**.
2. Разместите **PORT1** слева от R_drv_A (ещё левее):
   - Дважды щёлкните по PORT → задайте:
     - **PRT (Port Number):** `1`
     - **Z (Impedance):** `50` Ом
     - **Pwr:** `0` dBm (по умолчанию)
   - **OK**.
3. Проводом соедините: **PORT1** → левый вывод R_drv_A.
4. Разместите **PORT2** слева от R_drv_B:
   - **PRT:** `2`
   - **Z:** `50` Ом
5. Проводом соедините: **PORT2** → левый вывод R_drv_B.

**Зачем:** PORT1 и PORT2 — два одиночных порта, которые позже будут объединены в один дифференциальный порт при настройке измерений. PORT1 подаёт сигнал на линию A, PORT2 — на линию B. Вместе они формируют дифференциальный источник.

**Результат:** схема слева: PORT1 → R_drv_A → CLIN(1), PORT2 → R_drv_B → CLIN(2).

### Шаг 7. Добавление терминирующего резистора (R_term)

1. Добавьте элемент **RES**.
2. Разместите его **вертикально** справа от CLIN, между выводами 3 (верх) и 4 (низ).
   - Если элемент горизонтален, нажмите **Ctrl+R** для поворота.
3. Дважды щёлкните → задайте:
   - **R:** `120` Ом
   - **ID:** `R_term`
4. **OK**.
5. Проводами соедините:
   - Верхний вывод R_term → вывод 3 CLIN (выход линии A).
   - Нижний вывод R_term → вывод 4 CLIN (выход линии B).

**Зачем:** R_term = 120 Ом — терминирующий резистор, включённый **между линиями A и B** на приёмном конце. Его номинал равен дифференциальному волновому сопротивлению линии (Zdiff = 120 Ом). Это обеспечивает согласование и подавление отражений.

**Результат:** справа от CLIN вертикальный резистор 120 Ом соединяет выводы 3 и 4.

### Шаг 8. Добавление модели приёмника (R_rcv + C_rcv)

#### Верхняя ветвь (линия A, вход приёмника A):

1. Добавьте **RES** (R_rcv_A):
   - Разместите **вертикально** правее R_term, подключив к узлу линии A (вывод 3 CLIN / верх R_term).
   - R = `12000` Ом (12 кОм)
2. Добавьте **GND** под нижним выводом R_rcv_A. Соедините проводом.
3. Добавьте **CAP** (Capacitor):
   - Путь: **Circuit Elements → Lumped Elements → Capacitor → CAP**.
   - Разместите **параллельно** R_rcv_A (вертикально, рядом с ним).
   - C = `15` пФ (или `0.015` в единицах пФ, зависит от настроек единиц AWR).
   - В AWR значение ёмкости задаётся в пФ по умолчанию: введите `15`.
4. Соедините проводами:
   - Верхний вывод CAP → тот же узел, что и верхний вывод R_rcv_A (линия A).
   - Нижний вывод CAP → GND (тот же, что и у R_rcv_A).

#### Нижняя ветвь (линия B, вход приёмника B):

5. Аналогично добавьте **RES** (R_rcv_B):
   - R = `12000` Ом
   - Подключён от узла линии B (вывод 4 CLIN / низ R_term) к GND.
6. Добавьте **CAP** (C_rcv_B):
   - C = `15` пФ
   - Параллельно R_rcv_B.
7. Подключите GND.

**Зачем:** R_rcv моделирует входное сопротивление приёмника RS-485 (≥ 12 кОм по стандарту). C_rcv — входная ёмкость приёмника (типичные значения 10–30 пФ). Ёмкость создаёт RC-цепь, которая сглаживает фронты сигнала и влияет на высокочастотное поведение.

**Результат:** на приёмном конце каждая линия нагружена параллельной цепью R(12к)||C(15пФ) на землю, и между линиями стоит R_term(120 Ом).

### Шаг 9. Добавление выходных портов PORT3 и PORT4 (для 4-портового анализа)

Для получения смешанных (mixed-mode) S-параметров и наблюдения сигнала на приёмнике нужны ещё два порта:

1. Добавьте **PORT3**:
   - Разместите справа от узла линии A (после точки соединения R_term/R_rcv_A/C_rcv_A).
   - Подключите к этому узлу **через короткий провод** или напрямую.
   - **PRT:** `3`, **Z:** `1e6` Ом (1 МОм — «пробник», высокоомный порт, чтобы не влиять на схему).

   > **Важно:** PORT3 должен быть подключён в **ту же точку** (узел), что и R_term(верх), R_rcv_A и C_rcv_A. Все четыре элемента сходятся в одном узле.

2. Добавьте **PORT4**:
   - Аналогично, к узлу линии B.
   - **PRT:** `4`, **Z:** `1e6` Ом.

**Зачем:** PORT3 и PORT4 позволяют измерить напряжение на приёмном конце линии A и B. Высокий импеданс (1 МОм) делает их «невидимыми» для схемы — они не вносят нагрузку, только наблюдают.

> **Альтернативный подход (если не нужны PORT3/PORT4):** можно обойтись без них, но тогда нельзя будет построить Sdd21 (дифференциальная передача от входа к выходу). Для полноценного анализа все 4 порта рекомендуются.

**Результат:** 4-портовая схема: PORT1,PORT2 — вход (драйвер); PORT3,PORT4 — выход (приёмник).

### Шаг 10. Проверка схемы — общий вид

Итоговая схема должна выглядеть так (текстовая диаграмма):

```
PORT1 ─── R_drv_A(50Ω) ───┐                         ┌─── R_rcv_A(12kΩ) ─── GND
  (Z=50Ω)                  │     CLIN                │ ║
                            ├── вывод1 → вывод3 ──────┼─── C_rcv_A(15пФ) ─── GND
                            │   (линия A)             │
                            │                    R_term(120Ω)  ─── PORT3 (Z=1МОм)
                            │                         │
                            ├── вывод2 → вывод4 ──────┼─── PORT4 (Z=1МОм)
PORT2 ─── R_drv_B(50Ω) ───┘                         │ ║
  (Z=50Ω)                       (линия B)            ├─── R_rcv_B(12kΩ) ─── GND
                                                      │ ║
                            GND    GND                └─── C_rcv_B(15пФ) ─── GND
                             │      │
                          (земля CLIN)
```

Проверьте:
- Нет неподключённых (висящих) выводов. В AWR они обозначаются красным маркером.
- Все GND подключены.
- Все провода реально соединены (в точках соединения видны чёрные точки — узлы).
- CLIN имеет 4 подключённых сигнальных вывода и 2 заземлённых вывода.

### Шаг 11. Настройка частотного диапазона

1. В дереве проекта: правой кнопкой по имени проекта → **Options...** → вкладка **Frequencies**.
   Или: **Project → Project Options** → вкладка **Frequencies**.
   Или: **Simulate → Frequency...**.
2. Задайте:
   - **Start:** `0.0001` ГГц (= 100 кГц — нижняя граница для низкочастотного поведения)
   - **Stop:** `0.1` ГГц (= 100 МГц — до 10-й гармоники основной частоты 10 Мбит/с)
   - **Step:** `0.0005` ГГц (= 500 кГц — достаточно мелкий шаг для 200 точек)
3. **Apply** → **OK**.

**Зачем:** диапазон 0.1–100 МГц охватывает всю рабочую полосу RS-485 и выше. 200 частотных точек обеспечивают гладкие графики.

**Результат:** частотный диапазон задан в проекте.

---

## 4. Стартовые параметры модели

### 4.1. Полная таблица параметров

| Параметр | Элемент | Значение | Единица | Обоснование |
|----------|---------|----------|---------|-------------|
| Z порта 1 | PORT1 | 50 | Ом | Стандартный импеданс, согласован с R_drv |
| Z порта 2 | PORT2 | 50 | Ом | Аналогично |
| R_drv_A | RES | 50 | Ом | Выходное сопротивление драйвера; реальные драйверы: 30–60 Ом. 50 Ом — удобное начальное значение |
| R_drv_B | RES | 50 | Ом | Аналогично |
| Z0E (чётная мода) | CLIN | 90 | Ом | Синфазный импеданс; определяет Zcomm = Z0E/2 = 45 Ом |
| Z0O (нечётная мода) | CLIN | 60 | Ом | Дифференциальный: Zdiff = 2×Z0O = 120 Ом (стандарт RS-485) |
| EL | CLIN | 60 | градусы | Электрическая длина ≈ 6.6 м кабеля |
| F0 | CLIN | 0.005 | ГГц | 5 МГц — основная частота RS-485 (10 Мбит/с) |
| R_term | RES | 120 | Ом | Стандартный терминатор RS-485, равен Zdiff |
| R_rcv_A | RES | 12000 | Ом | Входное сопротивление приёмника (≥12 кОм по стандарту) |
| R_rcv_B | RES | 12000 | Ом | Аналогично |
| C_rcv_A | CAP | 15 | пФ | Входная ёмкость приёмника (типично 10–30 пФ) |
| C_rcv_B | CAP | 15 | пФ | Аналогично |
| Z порта 3 | PORT3 | 1000000 | Ом | Высокоомный «пробник» — не влияет на схему |
| Z порта 4 | PORT4 | 1000000 | Ом | Аналогично |
| Fstart | Project | 0.0001 | ГГц | 100 кГц — нижняя граница |
| Fstop | Project | 0.1 | ГГц | 100 МГц — верхняя граница |
| Fstep | Project | 0.0005 | ГГц | 500 кГц — шаг |

### 4.2. Влияние каждого параметра на результат

| Параметр | Влияние | Что произойдёт при изменении |
|----------|---------|------------------------------|
| R_drv | Определяет согласование со стороны драйвера | Увеличение → больше потерь, но меньше звонов |
| Z0O (→Zdiff) | Основной параметр линии | Если ≠ R_term → рассогласование → отражения |
| Z0E | Определяет синфазный режим | Влияет на подавление синфазной помехи |
| EL | Длина кабеля | Больше EL → больше задержка, больше фаза S21 |
| R_term | Согласование линии | Если R_term ≠ Zdiff → отражения |
| R_rcv | Нагрузка приёмника | Высокий R_rcv → почти не влияет на согласование; при малом R_rcv → дополнительная нагрузка |
| C_rcv | Частотная зависимость | Увеличение → спад S21 на высоких частотах, сглаживание фронтов |

### 4.3. Рекомендации для первой успешной симуляции

1. **Используйте именно указанные значения** — они подобраны для получения наглядных результатов.
2. **Не изменяйте Z0E и Z0O одновременно** — сначала убедитесь, что модель работает с базовыми значениями.
3. **Начинайте с EL = 60°**, а не 90° или больше — при небольшой длине линии графики более «читаемые».
4. **C_rcv = 15 пФ** даёт умеренный эффект — заметный, но не подавляющий. Это хорошее учебное значение.

---

## 5. Инструкция по моделированию

### 5.1. Тип анализа

Для данной модели используется **линейный частотный анализ (S-Parameter Analysis)** — это основной и наиболее надёжный режим работы AWR DE 17.

**Почему S-параметрический анализ:**
- AWR DE 17 — это прежде всего инструмент для СВЧ-моделирования, и S-параметрический анализ — его сильнейшая сторона.
- 4-портовый анализ CLIN даёт полную S-матрицу 4×4, из которой можно извлечь все необходимые характеристики.
- Смешанные (mixed-mode) S-параметры позволяют анализировать дифференциальный и синфазный режимы.
- Из частотной характеристики можно получить временну́ю через обратное преобразование Фурье (TDR).

**Нужен ли анализ переходных процессов (Transient)?**

AWR DE 17 включает транзиентный солвер APLAC, но для данной задачи S-параметрический анализ предпочтительнее:
- Он не требует дополнительных лицензий.
- Он точнее для линейных пассивных цепей.
- Временну́ю информацию (импульсный отклик, задержку) можно получить из S-параметров.

**Гармонический баланс (Harmonic Balance)?** Не нужен — в модели нет нелинейных элементов.

### 5.2. Создание графиков и измерений

#### График 1: Дифференциальный коэффициент отражения Sdd11

1. В дереве проекта правой кнопкой по **Graphs** → **New Graph**.
2. **Graph Name:** `Sdd11_Diff_Return_Loss`
3. **Graph Type:** `Rectangular`
4. **OK** → откроется пустой график.
5. Правой кнопкой внутри графика → **Add Measurement**.
6. В диалоге:
   - **Measurement Type:** `Port Parameters` → `S`
   - **Measurement:** `S[1,1]` (или `S(1,1)`)
   - **Data Source:** `RS485_Diff_Line`
   - **Complex Modifier:** `DB`

   > **Для mixed-mode S-параметров** (Sdd11): если AWR поддерживает смешанный режим в настройках измерения, выберите **Measurement Type → Port Parameters → Mixed-Mode S** (или **Sdd**), затем **Sdd[1,1]**. Если такой опции нет, используйте обычный S[1,1] и S[3,3] — для двухпортовой эквивалентной модели они несут ту же информацию.

   > **Альтернативный путь к mixed-mode:** В AWR DE 17 смешанные S-параметры доступны через определение балансных (balanced) портов. В настройках схемы (правой кнопкой по схеме в дереве → **Options** → **Port Group**) можно определить PORT1+PORT2 как дифференциальный порт D1, и PORT3+PORT4 как дифференциальный порт D2. После этого в измерениях станут доступны Sdd, Sdc, Scd, Scc.

7. **OK**.

#### График 2: Дифференциальный коэффициент передачи S[3,1] / Sdd21

1. Создайте новый **Rectangular** график: `S31_Diff_Insertion_Loss`.
2. Добавьте измерение:
   - **Measurement:** `S[3,1]` (передача от PORT1 к PORT3 — от входа A к выходу A)
   - **Complex Modifier:** `DB`
3. Добавьте **второе измерение** на тот же график:
   - **Measurement:** `S[4,2]` (передача от PORT2 к PORT4 — от входа B к выходу B)
   - **Complex Modifier:** `DB`

**Зачем:** S[3,1] показывает, какая доля сигнала от передатчика (линия A) доходит до приёмника (линия A). Аналогично S[4,2] для линии B. Для симметричной схемы они должны совпадать.

#### График 3: Прямой S11 — коэффициент отражения от PORT1

1. График: `S11_Port1_Return`.
2. Измерение: `S[1,1]`, **DB**.

**Зачем:** показывает отражение от входа линии A (со стороны драйвера). Включает влияние R_drv, CLIN, терминатора и нагрузки.

#### График 4: Фазы S[3,1] и S[4,2]

1. График: `Phase_S31_S42`.
2. Измерения:
   - `S[3,1]`, **Complex Modifier:** `Ang` (Angle, в градусах).
   - `S[4,2]`, **Ang**.

**Зачем:** линейный набег фазы подтверждает правильную задержку в линии. Обе кривые должны совпадать (симметрия).

#### График 5: Групповая задержка

1. График: `Group_Delay`.
2. Измерение: `S[3,1]`, **Complex Modifier:** `GrpDly` (или `GD`).

**Зачем:** постоянная групповая задержка подтверждает, что линия не вносит частотной дисперсии. Значение задержки должно соответствовать расчётному: τ = EL / (360 × F0) = 60 / (360 × 5×10⁶) = **33.3 нс**.

#### График 6: КСВН (VSWR)

1. График: `VSWR_Input`.
2. Измерение:
   - **Measurement Type:** `Port Parameters` → `VSWR`
   - **Port:** 1 (или `VSWR[1]`)

#### График 7: Связь между линиями S[3,2] / S[4,1] (перекрёстные наводки)

1. График: `Crosstalk`.
2. Измерения:
   - `S[3,2]`, **DB** — передача от PORT2 (вход B) к PORT3 (выход A).
   - `S[4,1]`, **DB** — передача от PORT1 (вход A) к PORT4 (выход B).

**Зачем:** для связанных линий эти параметры показывают перекрёстные наводки (crosstalk). Для витой пары RS-485 они должны быть значительными (сильная связь — это нормально для дифференциальной пары).

### 5.3. Настройка смешанных (Mixed-Mode) S-параметров

Это ключевая функция для дифференциального анализа.

1. В дереве проекта: правой кнопкой по схеме `RS485_Diff_Line` → **Options** (или **Properties**).
2. Найдите вкладку или раздел **Port Grouping** (**Balanced Ports**, **Mixed-Mode**).
3. Определите две группы портов:
   - **Дифференциальный порт D1 (вход):** PORT1 (+), PORT2 (−)
   - **Дифференциальный порт D2 (выход):** PORT3 (+), PORT4 (−)
4. **Apply** → **OK**.

Теперь в измерениях станут доступны:

| Параметр | Обозначение | Что показывает |
|----------|-------------|----------------|
| **Sdd11** | Дифф. отражение на входе | Отражение дифференциального сигнала от входа линии |
| **Sdd21** | Дифф. передача | Передача дифференциального сигнала от входа к выходу |
| **Scc11** | Синфазное отражение | Отражение синфазной помехи |
| **Scc21** | Синфазная передача | Передача синфазной помехи |
| **Sdc21** | Конверсия мод (дифф→синф) | Преобразование дифференциального сигнала в синфазный |
| **Scd21** | Конверсия мод (синф→дифф) | Преобразование синфазной помехи в дифференциальный шум |

**Создайте дополнительные графики:**

#### График 8: Sdd11 и Sdd21

1. График: `Mixed_Mode_Sdd`.
2. Измерения:
   - `Sdd[1,1]` (или `Sdd(1,1)`), **DB**
   - `Sdd[2,1]`, **DB**

#### График 9: Scc11 и Scc21

1. График: `Mixed_Mode_Scc`.
2. Измерения:
   - `Scc[1,1]`, **DB**
   - `Scc[2,1]`, **DB**

> **Примечание:** если в AWR DE 17 mixed-mode параметры настраиваются иначе (через меню Measurement → Mode Conversion или подобное), следуйте подсказкам интерфейса. Логика: указать, какие порты составляют балансную пару, затем выбрать Sdd/Scc/Sdc/Scd.

### 5.4. Запуск симуляции

1. Нажмите **Simulate → Analyze** (или **F8**, или кнопку с зелёной стрелкой ▶ на панели инструментов).
2. В строке состояния внизу отобразится прогресс.
3. Для данной модели расчёт занимает < 1 секунды.

**Результат:** все графики заполнятся кривыми.

### 5.5. Где и что смотреть

| Точка измерения | Параметр AWR | Что показывает |
|-----------------|--------------|----------------|
| Сигнал на выходе передатчика (после R_drv) | S[1,1] дБ | Насколько хорошо драйвер согласован с линией |
| Сигнал на входе приёмника | S[3,1] дБ или Sdd21 дБ | Сколько сигнала дошло до приёмника |
| Дифференциальное напряжение между A и B | Sdd21 (из mixed-mode) | Передача дифференциального сигнала |
| Отражения на линии | S[1,1] дБ или Sdd11 дБ | Амплитуда отражённого сигнала |
| Задержка распространения | Group Delay S[3,1] | Время прохождения сигнала по линии |
| Синфазная помеха | Scc21 дБ | Проходит ли синфазная помеха через линию |
| Перекрёстные наводки | S[3,2], S[4,1] | Связь между линиями |

---

## 6. Проверка работоспособности модели

### 6.1. Критерии корректной модели (базовый случай: терминатор 120 Ом)

| № | Критерий | Ожидаемое значение | Где проверить | Что означает |
|---|----------|--------------------|---------------|--------------|
| 1 | S[3,1] (дБ) | −6 ... −12 дБ, слабая зависимость от частоты до 10 МГц | График S31_Diff_Insertion_Loss | Сигнал передаётся с потерями из-за делителя напряжения R_drv / (R_drv + Z_line) |
| 2 | S[1,1] (дБ) | < −10 дБ (желательно < −15 дБ) в рабочей полосе | График S11_Port1_Return | Приемлемое согласование со стороны драйвера |
| 3 | Фаза S[3,1] | Линейно убывает с частотой | График Phase_S31_S42 | Сигнал задерживается в линии — это нормально |
| 4 | S[3,1] и S[4,2] | Совпадают (разница < 0.1 дБ) | На одном графике | Симметрия линии — обе стороны витой пары работают одинаково |
| 5 | Group Delay | ≈ 33 нс, постоянна | График Group_Delay | Задержка соответствует расчётной, дисперсии нет |
| 6 | Sdd21 (дБ) | Близок к S[3,1], слабо зависит от частоты | Mixed-mode график | Дифференциальная передача работает |
| 7 | Scc21 (дБ) | Значительно ниже Sdd21 | Mixed-mode график | Синфазный сигнал подавляется (это нормально для дифференциальной передачи) |
| 8 | VSWR | 1.5–3.0 в рабочей полосе | График VSWR | Умеренное рассогласование из-за R_drv ≠ Zdiff/2 — ожидаемо |

> **Важное замечание:** S[3,1] **не будет** равен 0 дБ! В модели присутствует делитель напряжения: R_drv (50 Ом) + Z_line (60 Ом нечётной моды). Часть мощности отражается от входа линии. Потери передачи ~6–10 дБ в рабочей полосе — это нормально для данной схемы и не означает ошибку.

### 6.2. Проверка дифференциальной передачи

**Как убедиться, что сигнал действительно дифференциальный:**

1. S[3,1] и S[4,2] должны **совпадать** — это означает, что обе линии работают симметрично.
2. Фазы S[3,1] и S[4,2] должны **совпадать** — одинаковая задержка по обоим проводникам.
3. Sdd21 должен быть **существенно больше** (ближе к 0 дБ), чем Sdc21 (конверсия мод) — дифференциальный сигнал передаётся лучше, чем преобразуется в синфазный.

**Если S[3,1] ≠ S[4,2]:** проверьте симметрию схемы — одинаковы ли R_drv_A и R_drv_B, R_rcv_A и R_rcv_B, C_rcv_A и C_rcv_B.

### 6.3. Проверка влияния терминатора — 5 ключевых экспериментов

#### Эксперимент 1: С терминатором R_term = 120 Ом (штатный режим)

Это базовая модель. Ожидается: S[1,1] < −10 дБ, S[3,1] плавная кривая, Group Delay = const.

#### Эксперимент 2: Без терминатора (R_term → ∞)

1. Удалите R_term из схемы (или измените R = `1e9` Ом — практически обрыв).
2. Запустите F8.

**Ожидается:**
- S[1,1] резко возрастает — появляются сильные периодические осцилляции (стоячие волны).
- S[3,1] также осциллирует с частотой.
- VSWR >> 3.

**Что это доказывает:** без терминатора происходит практически полное отражение от конца линии (Γ ≈ 1). Стоячие волны наглядно видны.

#### Эксперимент 3: Неправильный терминатор (R_term = 50 Ом)

1. Измените R_term = `50` Ом.
2. F8.

**Ожидается:**
- S[1,1] хуже, чем при 120 Ом — появляются осцилляции, но менее выраженные, чем без терминатора.
- S[3,1] снижается (больше мощности поглощается терминатором, меньше доходит до высокоомного приёмника... на самом деле картина сложнее из-за рассогласования).

**Что это доказывает:** номинал терминатора должен быть **точно равен** Zdiff линии.

#### Эксперимент 4: Увеличенная длина линии (EL = 360°, ~40 м)

1. Верните R_term = 120 Ом.
2. Измените EL CLIN на `360` градусов.
3. F8.

**Ожидается:**
- Фаза S[3,1] нарастает значительно быстрее.
- Group Delay увеличивается пропорционально (≈ 200 нс).
- На высоких частотах (> 30 МГц) начинает проявляться влияние C_rcv — спад S[3,1].
- Общая картина согласования не меняется (если R_term = Zdiff).

**Что это доказывает:** модель корректно масштабируется по длине. Задержка пропорциональна длине.

#### Эксперимент 5: Увеличенная ёмкость приёмника (C_rcv = 100 пФ)

1. Верните EL = 60°.
2. Измените C_rcv_A и C_rcv_B на `100` пФ.
3. F8.

**Ожидается:**
- На низких частотах (< 5 МГц) картина не меняется.
- На высоких частотах (> 20 МГц) S[3,1] и S[4,2] начинают заметно падать.
- Sdd21 тоже снижается на высоких частотах.
- Появляется частотная зависимость Sdd11 (ухудшение согласования на ВЧ).

**Что это доказывает:** входная ёмкость приёмника создаёт фильтр нижних частот, ограничивающий пропускную способность линии. В реальной системе это ограничивает максимальную скорость передачи.

### 6.4. Проверка задержки распространения

1. Посмотрите значение Group Delay на графике.
2. Расчётное значение: τ = EL / (360 × F0) = 60 / (360 × 5×10⁶) = **33.3 нс**.
3. Значение на графике должно быть ≈ 33 нс (с небольшими колебаниями из-за R_drv и C_rcv).
4. Если значение сильно отличается — проверьте F0 (должно быть 0.005 ГГц) и EL (60°).

### 6.5. Признаки неправильной модели

| Симптом на графике | Вероятная причина | Как исправить |
|--------------------|-------------------|---------------|
| Все S-параметры = 0 дБ или −∞ дБ | Оборван провод, порт не подключён | Проверить все соединения |
| S[3,1] = −∞ дБ (нет передачи) | Разрыв в цепи: R_drv или CLIN не подключён | Проверить провода от PORT к CLIN |
| S[3,1] > 0 дБ | Ошибка — усиление в пассивной цепи невозможно | Проверить параметры элементов; возможно, PORT3/PORT4 имеют слишком малый Z |
| S[1,1] = 0 дБ на всех частотах | Полное отражение — CLIN не подключён или имеет нулевую длину | Проверить EL > 0 и подключение |
| S[3,1] не зависит от частоты вообще | EL = 0 (линия нулевой длины = провод) | Задать EL > 0 |
| Group Delay = 0 | EL = 0 или F0 не задана | Проверить EL и F0 |
| S[3,1] ≠ S[4,2] (большая разница) | Несимметрия: разные R_drv, R_rcv или C_rcv | Проверить все парные элементы |
| Ошибка «Floating node» | Неподключённый вывод | Все земляные выводы CLIN → GND; все элементы подсоединены |
| Ошибка «Duplicate port number» | Два порта с одинаковым номером | Проверить PRT: 1, 2, 3, 4 — все разные |
| Пустые графики после F8 | Нет измерений на графиках, или Data Source неправильный | Правой кнопкой → Add Measurement; проверить Data Source = имя схемы |

### 6.6. Как выявить ошибки полярности

В модели нет явной «полярности» как в реальной микросхеме (пин A и пин B). Но если перепутать подключение (PORT1 → линия B, PORT2 → линия A), то:
- S[3,1] и S[4,2] **поменяются местами** с S[3,2] и S[4,1].
- В дифференциальном режиме (Sdd) результат не изменится (т.к. смена полярности обоих входов и выходов одновременно не меняет Sdd).
- Но если поменять **только один** вход или выход, то Sdd21 сильно изменится, и Sdc21 (конверсия мод) станет большим — это признак ошибки полярности.

---

## 7. Обязательные результаты для учебной работы

### 7.1. Список результатов

| № | Результат | Формат | Зачем нужен | Вывод |
|---|-----------|--------|-------------|-------|
| 1 | S[1,1] (дБ) vs частота, с терминатором | График | Показать качество согласования со стороны драйвера | Согласование достаточно хорошее (< −10 дБ), линия работает |
| 2 | S[3,1] (дБ) vs частота, с терминатором | График | Показать передачу сигнала от драйвера к приёмнику | Сигнал передаётся с умеренными потерями, ровная кривая |
| 3 | S[1,1] (дБ) vs частота, **без** терминатора | График | Показать стоячие волны при рассогласовании | Сильные осцилляции, отражения |
| 4 | S[3,1] (дБ) vs частота, **без** терминатора | График | Показать искажение передаточной характеристики | Осцилляции, неравномерность |
| 5 | Фаза S[3,1] vs частота | График | Показать задержку в линии | Линейный набег фазы — подтверждение корректной длины линии |
| 6 | Group Delay vs частота | График | Подтвердить расчётную задержку | Постоянное значение ≈ 33 нс |
| 7 | S[3,1] при разных EL (60°, 180°, 360°) | Несколько кривых | Показать влияние длины кабеля | Задержка растёт с длиной, ВЧ-потери увеличиваются |
| 8 | S[3,1] при C_rcv = 0, 15 пФ, 100 пФ | Несколько кривых | Показать влияние паразитной ёмкости | Спад на ВЧ увеличивается с ростом ёмкости |
| 9 | Sdd21 и Scc21 (если mixed-mode настроен) | График | Разделение дифференциального и синфазного режимов | Дифференциальный сигнал передаётся значительно лучше синфазного |
| 10 | VSWR vs частота | График | Показать коэффициент стоячей волны | VSWR < 3 с терминатором, VSWR >> 3 без него |

### 7.2. Как разместить несколько кривых на одном графике (для сравнения)

Для сравнения «с терминатором» / «без терминатора»:

**Способ 1: Tuning (подстройка в реальном времени)**
1. Выберите R_term на схеме.
2. **Simulate → Tune** (или горячая клавиша).
3. Двигайте ползунок R от 120 до 100000 Ом — графики обновляются в реальном времени.

**Способ 2: Использование переменных (Variables)**
1. В дереве проекта: **Global Definitions** → добавьте переменную: `R_TERM = 120`.
2. В свойствах R_term задайте R = `R_TERM` (вместо числа).
3. Измените значение переменной → перезапустите F8 → сохраните график.
4. Измените значение → F8 → сохраните второй график.

**Способ 3: Sweep (параметрическое моделирование)**
1. В дереве проекта: правой кнопкой по схеме → **Options** → **Parameter Sweep** (или **Simulate → Sweep**).
2. Задайте: параметр R_TERM, значения: 120, 1e6 (или 120, 50, 1e6).
3. Запустите F8 — на графике появятся несколько кривых одновременно.

### 7.3. Интерпретация для отчёта

#### Вывод по S-параметрам (с терминатором):

> Результаты S-параметрического анализа модели RS-485 показывают, что при наличии согласующего терминатора 120 Ом коэффициент передачи S[3,1] (от драйвера к приёмнику) составляет от −8 до −10 дБ в рабочей полосе частот 0.1–10 МГц. Потери обусловлены делителем напряжения, образованным выходным сопротивлением драйвера (50 Ом) и входным импедансом линии. Коэффициент отражения S[1,1] < −12 дБ, что свидетельствует о приемлемом согласовании. Групповая задержка составляет ≈33 нс, что соответствует расчётной длине кабеля ~6.6 м.

#### Вывод по сравнению согласованной и несогласованной линии:

> При удалении терминатора (эквивалент обрыва нагрузки) коэффициент отражения S[1,1] возрастает до ~0 дБ с выраженной периодической структурой, характерной для стоячих волн. Коэффициент передачи S[3,1] приобретает осциллирующий характер. Это подтверждает, что для корректной работы интерфейса RS-485 терминирующий резистор, равный дифференциальному волновому сопротивлению линии (120 Ом), является обязательным.

#### Вывод по влиянию параметров:

> Увеличение длины кабеля приводит к пропорциональному росту задержки распространения сигнала, но не влияет на амплитуду при наличии согласования. Увеличение входной ёмкости приёмника (с 15 пФ до 100 пФ) создаёт фильтр нижних частот, ограничивающий полосу пропускания канала. Это объясняет ограничение скорости передачи RS-485 при использовании приёмников с высокой входной ёмкостью.

---

## 8. Типичные ошибки

### 8.1. Полный каталог ошибок

#### 1. Неправильный тип линии

| Ошибка | Использование TLIN вместо CLIN |
|--------|-------------------------------|
| **Как проявляется** | Модель работает, но нет двух линий A и B; нельзя подключить терминатор между линиями; невозможно анализировать mixed-mode |
| **Как заметить** | На схеме только один проводник; нет перекрёстных S-параметров |
| **Как исправить** | Заменить TLIN на CLIN; перестроить схему для 4 портов |

#### 2. Неверное волновое сопротивление

| Ошибка | Z0O ≠ 60 Ом (→ Zdiff ≠ 120 Ом) |
|--------|-------------------------------|
| **Как проявляется** | Терминатор 120 Ом не согласует линию; на S11 видны осцилляции даже с терминатором |
| **Как заметить** | S[1,1] > −10 дБ с терминатором 120 Ом |
| **Как исправить** | Пересчитать: Z0O = Zdiff/2 = 60 Ом, R_term = Zdiff = 2×Z0O |

#### 3. Отсутствие терминатора

| Ошибка | R_term не подключён или не установлен |
|--------|-------------------------------|
| **Как проявляется** | Сильные осцилляции S11 и S21, VSWR >> 3 |
| **Как заметить** | Периодическая структура на всех графиках |
| **Как исправить** | Добавить RES(120 Ом) между линиями A и B на приёмном конце |

#### 4. Терминатор подключён к земле, а не между линиями

| Ошибка | R_term от линии A к GND (вместо A к B) |
|--------|-------------------------------|
| **Как проявляется** | Терминатор не согласует дифференциальный импеданс; графики похожи на случай без терминатора |
| **Как заметить** | Sdd11 > −10 дБ; Scc11 может улучшиться (синфазное согласование), но Sdd не улучшается |
| **Как исправить** | Подключить R_term **между** выходами A и B, а не к GND |

#### 5. Неправильная электрическая длина / единицы F0

| Ошибка | F0 = 5 (подразумевалось МГц, а AWR ожидает ГГц → получилось 5 ГГц) |
|--------|-------------------------------|
| **Как проявляется** | Линия ведёт себя как в 1000 раз более короткая; задержка ~0.03 нс вместо 33 нс |
| **Как заметить** | Group Delay ≈ 0; фаза S21 почти не меняется |
| **Как исправить** | F0 = `0.005` ГГц (не 5!) — в AWR единица частоты — ГГц |

#### 6. Неправильное задание дифференциального сигнала

| Ошибка | PORT1 и PORT2 подключены к одной и той же линии (обе к A) |
|--------|-------------------------------|
| **Как проявляется** | Нет дифференциальной передачи; Sdd → 0; модель ведёт себя как однопроводная |
| **Как заметить** | S[3,1] и S[3,2] одинаковы; S[4,1] и S[4,2] одинаковы |
| **Как исправить** | PORT1 → вход линии A (вывод 1 CLIN), PORT2 → вход линии B (вывод 2 CLIN) |

#### 7. Перепутана полярность (PORT1 на B, PORT2 на A)

| Ошибка | PORT1 подключён к линии B, PORT2 к линии A |
|--------|-------------------------------|
| **Как проявляется** | Sdd результаты корректны (если оба конца перепутаны), но если только один конец — Sdc/Scd резко возрастает |
| **Как заметить** | Sdc21 ≈ Sdd21 (конверсия мод сравнима с передачей) — признак ошибки полярности |
| **Как исправить** | Убедиться, что PORT1→A, PORT2→B и PORT3→A, PORT4→B |

#### 8. Отсутствие связи между проводниками (Z0E = Z0O)

| Ошибка | Z0E = Z0O = 60 Ом (связь = 0) |
|--------|-------------------------------|
| **Как проявляется** | CLIN вырождается в две независимые линии; нет перекрёстных наводок |
| **Как заметить** | S[3,2] ≈ −∞ дБ (нет связи); k = 0 |
| **Как исправить** | Задать Z0E > Z0O. Для RS-485: Z0E = 90 Ом, Z0O = 60 Ом |

#### 9. Неверный режим анализа

| Ошибка | Попытка запустить Harmonic Balance для линейной цепи |
|--------|-------------------------------|
| **Как проявляется** | Ошибка или крайне медленная симуляция |
| **Как заметить** | Сообщения об ошибке в лог-файле |
| **Как исправить** | Использовать штатный Linear / S-Parameter анализ (это анализ по умолчанию) |

#### 10. PORT3/PORT4 с малым импедансом

| Ошибка | Z порта 3 или 4 = 50 Ом (вместо высокоомного) |
|--------|-------------------------------|
| **Как проявляется** | Порты создают дополнительную нагрузку, искажая результаты; R_term «шунтирован» портами |
| **Как заметить** | S[3,1] значительно отличается от ожидаемого; снятие PORT3/PORT4 меняет картину |
| **Как исправить** | Задать Z PORT3 = Z PORT4 = 1e6 Ом (1 МОм) |

---

## 9. Печатная плата / топология

### 9.1. Витая пара (кабель) ≠ дифференциальная пара (на плате)

**Это принципиально разные вещи:**

| Характеристика | Витая пара (кабель RS-485) | Дифференциальная пара (на плате) |
|----------------|----------------------------|----------------------------------|
| Среда | Изолированные проводники в кабеле | Медные дорожки на печатной плате |
| Длина | 1–1200 м | 1–50 см (участок на плате) |
| Zdiff | 120 Ом (стандарт) | Зависит от геометрии дорожек (обычно 90 или 100 Ом, но можно задать 120 Ом) |
| Диэлектрик | Полиэтилен, воздух | FR-4, Rogers и т.д. |
| Потери | Малые (для коротких длин) | Зависят от частоты и материала |
| Модель в AWR | **CLIN** (связанные идеальные линии) | **MCLIN** (связанные микрополоски, требует MSUB) |

**В реальной системе RS-485 есть оба участка:**
1. Участок на плате: от микросхемы драйвера до разъёма (несколько сантиметров).
2. Кабель: от разъёма до разъёма (метры и десятки метров).
3. Участок на плате приёмника: от разъёма до микросхемы приёмника.

### 9.2. Что можно сделать в AWR DE 17

AWR DE 17 **позволяет:**
- Смоделировать **участок дифференциальной пары на плате** с помощью **MCLIN + MSUB**.
- Увидеть **топологию (Layout)** двух параллельных дорожек.
- Рассчитать ширину дорожек и зазор для целевого дифференциального импеданса.
- Провести электромагнитное моделирование (AXIEM).

AWR DE 17 **не позволяет:**
- Создать полноценную печатную плату с компонентами (микросхемами, разъёмами).
- Генерировать Gerber-файлы.
- Моделировать кабель витой пары как физический объект с геометрией скрутки.

### 9.3. Как корректно объяснить это в учебной работе

> В работе представлены две модели:
> 1. **Модель кабеля витой пары** — реализована с помощью элемента CLIN (идеальные связанные линии передачи), описывающего два электромагнитно связанных проводника с заданными импедансами чётной (Z0E = 90 Ом) и нечётной (Z0O = 60 Ом) мод. Эта модель соответствует участку кабеля RS-485.
> 2. **Модель участка печатной платы** — реализована с помощью элемента MCLIN (связанные микрополосковые линии) на подложке FR-4 (MSUB). Эта модель соответствует участку дифференциальной пары от микросхемы до разъёма.
>
> Полноценная печатная плата с размещением компонентов в AWR Design Environment не создаётся — для этого необходимо использовать специализированные САПР (Altium Designer, KiCad). AWR используется для расчёта импедансов линий и электромагнитного моделирования критических участков.

### 9.4. Пошаговая инструкция: модель участка платы (MCLIN)

#### Шаг 1. Создание отдельной схемы для платы

1. Правой кнопкой по **Circuit Schematics** → **New Schematic** → имя: `RS485_PCB_Section`.
2. Откроется новый лист.

#### Шаг 2. Добавление подложки MSUB

1. **Circuit Elements → Substrates → MSUB** → перетащите на схему.
2. Дважды щёлкните → задайте параметры FR-4:
   - **Er:** `4.6` (диэлектрическая проницаемость FR-4)
   - **H:** `1.5` мм (толщина диэлектрика; в mil: `59.06`)
   - **T:** `0.035` мм (толщина меди, 1 oz; в mil: `1.38`)
   - **Rho:** `1` (удельное сопротивление, нормировано к меди)
   - **Tand:** `0.02` (тангенс угла потерь FR-4)
   - **ErNom:** `4.6`
   - **Name:** `FR4`
3. **OK**.

**Зачем:** MSUB определяет физическую структуру платы. Без него MCLIN не может рассчитать импедансы.

#### Шаг 3. Расчёт ширины дорожек в TXLine

1. Меню: **Tools → TXLine...** (TX-Line Calculator).
2. Выберите тип линии: **Coupled Microstrip** (связанная микрополоска).
3. Задайте:
   - **Er:** 4.6
   - **H:** 1.5 мм
   - **T:** 0.035 мм
   - **Frequency:** 5 МГц (или 0.005 ГГц)
   - **Zdiff (целевой):** 120 Ом
4. Нажмите **Synthesize**.
5. Запишите полученные **W** (ширина дорожки) и **S** (зазор между дорожками).

**Ожидаемый результат:** для FR-4, H = 1.5 мм, Zdiff = 120 Ом ширина W ≈ 0.2–0.4 мм, зазор S ≈ 0.15–0.3 мм (точные значения зависят от версии калькулятора).

> Если TXLine не может синтезировать (например, невозможно получить Zdiff = 120 Ом при данных параметрах подложки), попробуйте:
> - Уменьшить H (0.5 мм вместо 1.5 мм).
> - Уменьшить W и увеличить S.
> - Или принять Zdiff = 100 Ом (менее стандартно, но технически реализуемо на FR-4).

#### Шаг 4. Размещение MCLIN

1. **Circuit Elements → Transmission Lines → Coupled Lines → MCLIN** → на схему.
2. Дважды щёлкните → параметры:
   - **W:** значение из TXLine (например, `0.3` мм)
   - **S:** значение из TXLine (например, `0.2` мм)
   - **L:** `30` мм (длина участка платы — 3 см)
   - **MSUB:** выберите `FR4`
3. **OK**.

#### Шаг 5. Сборка схемы

Схема аналогична основной модели с CLIN, но:
- Вместо CLIN используется MCLIN.
- Подложка MSUB должна быть на схеме (даже без подключения — MCLIN ссылается на неё по имени).

Подключите 4 порта и терминатор аналогично шагам 5–9 из раздела 3.

#### Шаг 6. Просмотр топологии Layout

1. Откройте схему `RS485_PCB_Section`.
2. Меню: **View → Layout** (или вкладка **Layout** внизу окна схемы).
3. AWR сгенерирует Layout автоматически.
4. На Layout вы увидите **две параллельные дорожки** длиной L, шириной W, с зазором S.

**Результат:** визуальное представление участка дифференциальной пары RS-485 на печатной плате.

#### Шаг 7. Настройка топологии

В Layout Editor можно:
- Изменить масштаб: **Ctrl + колесо мыши**.
- Уместить всё в окно: **Home** или **View → Fit All**.
- Измерить расстояния: **Tools → Measure** (если доступно).
- Экспортировать изображение: правой кнопкой → **Copy** или **File → Export**.

#### Шаг 8. EM-симуляция (для продвинутых)

Для электромагнитного моделирования топологии:

1. В дереве проекта: правой кнопкой по **EM Structures** → **New EM Structure** → имя: `RS485_EM`.
2. В EM Structure Editor задайте подложку (Stackup):
   - Добавьте слой диэлектрика (FR-4: Er = 4.6, H = 1.5 мм, Tand = 0.02).
   - Добавьте слой металла (медь: T = 0.035 мм, σ = 5.8×10⁷ С/м).
   - Добавьте земляной слой снизу.
3. Импортируйте геометрию из Layout схемы (или нарисуйте вручную две полоски).
4. Добавьте порты на концах дорожек.
5. Задайте частотный диапазон.
6. Запустите AXIEM: **Simulate → Analyze**.
7. Сравните результаты EM-модели с аналитической моделью MCLIN.

**Это опциональный шаг** — для базовой учебной работы схемотехническая модель с MCLIN достаточна.

### 9.5. Комплексная модель: плата + кабель + плата

Для наиболее реалистичной учебной модели можно соединить последовательно:

```
MCLIN(плата TX, 3 см) → CLIN(кабель, 10 м) → MCLIN(плата RX, 3 см)
```

Это создаётся на одной схеме путём каскадного соединения:
1. PORT1, PORT2 → R_drv → **MCLIN_TX** (участок платы передатчика)
2. Выходы MCLIN_TX → **CLIN** (кабель витой пары)
3. Выходы CLIN → **MCLIN_RX** (участок платы приёмника)
4. Выходы MCLIN_RX → R_term, R_rcv, C_rcv → PORT3, PORT4

Это показывает полный тракт передачи RS-485 от микросхемы до микросхемы.

---

## 10. Итоговый чек-лист

### Перед симуляцией

- [ ] Проект создан и сохранён.
- [ ] Схема `RS485_Diff_Line` создана.
- [ ] CLIN размещён: Z0E = 90, Z0O = 60, EL = 60°, F0 = 0.005 ГГц.
- [ ] Земляные выводы CLIN подключены к GND.
- [ ] R_drv_A (50 Ом) размещён между PORT1 и CLIN(1).
- [ ] R_drv_B (50 Ом) размещён между PORT2 и CLIN(2).
- [ ] PORT1: PRT=1, Z=50 Ом. PORT2: PRT=2, Z=50 Ом.
- [ ] R_term (120 Ом) подключён **между** CLIN(3) и CLIN(4).
- [ ] R_rcv_A (12 кОм) + C_rcv_A (15 пФ) параллельно, от линии A к GND.
- [ ] R_rcv_B (12 кОм) + C_rcv_B (15 пФ) параллельно, от линии B к GND.
- [ ] PORT3: PRT=3, Z=1e6. PORT4: PRT=4, Z=1e6.
- [ ] Все номера портов разные: 1, 2, 3, 4.
- [ ] Нет висящих (красных) выводов.
- [ ] Частотный диапазон задан: 0.0001–0.1 ГГц.
- [ ] Создан хотя бы один график с измерением.

### После симуляции (базовый случай с терминатором)

- [ ] S[3,1] ≈ −8…−10 дБ в рабочей полосе (0.1–10 МГц).
- [ ] S[1,1] < −10 дБ.
- [ ] S[3,1] ≈ S[4,2] (симметрия).
- [ ] Фаза S[3,1] линейно убывает.
- [ ] Group Delay ≈ 33 нс.
- [ ] Графики не пустые.

### Проверочные эксперименты

- [ ] Без терминатора (R_term → 1e9): S[1,1] осциллирует, VSWR >> 3.
- [ ] Неправильный терминатор (50 Ом): промежуточная картина.
- [ ] Длинная линия (EL = 360°): задержка увеличивается в 6 раз.
- [ ] Большая ёмкость (C_rcv = 100 пФ): спад S[3,1] на высоких частотах.

### Для отчёта

- [ ] Сохранены ≥ 8 графиков (S11, S31, фаза, задержка, с/без терминатора, влияние параметров).
- [ ] Записаны все параметры модели.
- [ ] Описана эквивалентная схема (таблица соответствия).
- [ ] Написаны выводы по каждому эксперименту.
- [ ] Объяснена разница между моделью кабеля (CLIN) и моделью платы (MCLIN).
- [ ] Приложена топология Layout (если создана модель платы).

---

## Приложение А. Справочные формулы

| Параметр | Формула | Пример для базовой модели |
|----------|---------|---------------------------|
| Дифф. импеданс | Zdiff = 2 × Z0O | 2 × 60 = **120 Ом** |
| Синфазный импеданс | Zcomm = Z0E / 2 | 90 / 2 = **45 Ом** |
| Коэффициент связи | k = (Z0E − Z0O) / (Z0E + Z0O) | (90 − 60) / (90 + 60) = **0.2** |
| Электрическая длина → физическая | L = EL × c / (360 × F0 × √εeff) | 60 × 3e8 / (360 × 5e6 × 1.52) = **6.58 м** |
| Физическая длина → электрическая | EL = 360 × L × F0 × √εeff / c | 360 × 10 × 5e6 × 1.52 / 3e8 = **91.2°** |
| Задержка распространения | τ = EL / (360 × F0) | 60 / (360 × 5e6) = **33.3 нс** |
| Коэффициент отражения (дифф.) | Γ = (R_term − Zdiff) / (R_term + Zdiff) | (120 − 120) / (120 + 120) = **0** |
| Коэффициент отражения (без терм.) | Γ = (R_rcv_diff − Zdiff) / (R_rcv_diff + Zdiff) | (6000 − 120) / (6000 + 120) ≈ **0.96** |
| КСВН | VSWR = (1 + |Γ|) / (1 − |Γ|) | (1 + 0) / (1 − 0) = **1.0** (идеал) |

**Обозначения:**
- Z0E, Z0O — импедансы чётной и нечётной мод
- εeff — эффективная диэлектрическая проницаемость (для витой пары ≈ 2.3, √εeff ≈ 1.52)
- c = 3 × 10⁸ м/с — скорость света
- R_rcv_diff = R_rcv_A ‖ R_rcv_B = R_rcv / 2 = 6000 Ом (при R_rcv = 12 кОм)

## Приложение Б. Типичные параметры RS-485 (EIA/TIA-485)

| Параметр | Значение |
|----------|----------|
| Дифференциальное волновое сопротивление кабеля | 120 ± 10 Ом |
| Терминирующий резистор | 120 Ом |
| Максимальная скорость передачи | 10 Мбит/с (50 Мбит/с для RS-485 Extended) |
| Максимальная длина линии | 1200 м (при скорости ≤ 100 кбит/с) |
| Выходное напряжение драйвера | ±1.5 В … ±6 В (дифференциальное) |
| Порог чувствительности приёмника | ±200 мВ (дифференциально) |
| Входное сопротивление приёмника | ≥ 12 кОм (1 unit load); ≥ 96 кОм (1/8 unit load) |
| Выходное сопротивление драйвера | ~10–60 Ом (зависит от микросхемы) |
| Входная ёмкость приёмника | ~10–30 пФ (типично ~15 пФ) |
| Число приёмников на шине | до 32 (1 UL) / до 256 (1/8 UL) |
| Тип кабеля | Витая пара, волновое сопротивление 120 Ом |
| Скорость распространения в кабеле | ~0.66c (для полиэтиленовой изоляции) |
| Эффективная εr кабеля | ~2.3 |

## Приложение В. Горячие клавиши AWR DE 17

| Действие | Клавиша |
|----------|---------|
| Запуск симуляции | **F8** |
| Добавить элемент | **Draw → Add Element** (или ввод имени в поле поиска Elements Browser) |
| Провод (Wire) | **W** или **Ctrl+W** |
| Увеличить масштаб | **Ctrl + колесо мыши** |
| Уместить в окно | **Home** |
| Повернуть элемент | **Ctrl+R** (до размещения) |
| Отразить элемент | **Ctrl+M** |
| Свойства элемента | **Двойной щелчок** |
| Удалить | **Delete** |
| Отмена | **Ctrl+Z** |
| Сохранить проект | **Ctrl+S** |
| Tune (подстройка) | **Simulate → Tune** |

## Приложение Г. Соответствие пинов CLIN в AWR

```
           CLIN
           ┌────────────────┐
Pin 1 ────►│  Линия 1 (A)   ├────► Pin 3
           │                │
Pin 2 ────►│  Линия 2 (B)   ├────► Pin 4
           └───┬────────┬───┘
               │        │
             GND_1    GND_2
```

- **Pin 1** → вход линии A (подключить через R_drv_A к PORT1)
- **Pin 2** → вход линии B (подключить через R_drv_B к PORT2)
- **Pin 3** → выход линии A (подключить к R_term(верх), R_rcv_A, C_rcv_A, PORT3)
- **Pin 4** → выход линии B (подключить к R_term(низ), R_rcv_B, C_rcv_B, PORT4)
- **GND_1, GND_2** → земля (подключить к GND)
