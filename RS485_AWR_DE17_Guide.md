# Моделирование линии RS-485 на витой паре в AWR Design Environment 17

## Полная практическая инструкция (исправленная версия)

---

## ВАЖНО: исправление ошибки с PORT3/PORT4

В предыдущей версии инструкции порты PORT3 и PORT4 имели импеданс Z = 1 МОм («пробники»). Это **грубая ошибка**, которая приводит к бессмысленным результатам:

**Симптом:** DB(S[3,1]) даёт значения от −77 до −117 дБ. Это означает, что «через линию проходит» лишь 10⁻⁴ ... 10⁻⁶ доля мощности — абсурд для линии без потерь.

**Причина:** S-параметры — это отношения **волн мощности**, нормированных к импедансу порта. Порт с Z = 1 МОм «ожидает» ток порядка V/1e6, поэтому мощность, которую он «поглощает» из схемы, ничтожна. Формально S[3,1] = b₃/a₁, где b₃ ∝ 1/√Z₃. При Z₃ = 1 МОм коэффициент √(Z₁/Z₃) = √(50/10⁶) ≈ 0.007, что даёт −43 дБ **только из-за разницы нормировки**, ещё до учёта реальных потерь в цепи. Поэтому −77 дБ — это не «сигнал не проходит», а **артефакт неправильной нормировки**.

**Исправление:** PORT3 и PORT4 должны иметь Z = 60 Ом (= Z0O, импеданс нечётной моды CLIN). При этом порты **сами выполняют роль терминирующей нагрузки** (дифференциальный импеданс 2 × 60 = 120 Ом), а отдельные элементы R_term, R_rcv, C_rcv на выходе **не нужны** в базовой модели.

---

## 1. Выбор модели: что именно строим

### 1.1. Что моделируется

Моделируется передача дифференциального сигнала RS-485: от драйвера через кабель витой пары к приёмнику.

Модель строится **поэтапно** — от простого к сложному:

| Этап | Модель | Что показывает |
|------|--------|----------------|
| **A. Базовая** | 4 порта (Z=60 Ом) + CLIN | Чистая характеристика линии, идеальное согласование |
| **B. С драйвером** | Добавляем R_drv на входе | Влияние выходного сопротивления драйвера |
| **C. Полная** | Добавляем R_term, R_rcv, C_rcv, убираем PORT3/PORT4 | Полная эквивалентная схема RS-485 (2-портовая) |

Рекомендуется начать с **модели A** — убедиться, что она работает (S[3,1] ≈ 0 дБ), а затем усложнять.

### 1.2. Почему CLIN

CLIN (Coupled Ideal Transmission Lines) — элемент AWR, представляющий **два электромагнитно связанных проводника**. Это корректное представление витой пары:

- Два проводника (линии A и B) с заданной электромагнитной связью.
- 4-портовый элемент: вход A, вход B, выход A, выход B.
- Задаёт дифференциальный импеданс через импедансы мод: Zdiff = 2 × Z0O.
- Позволяет анализировать и дифференциальный, и синфазный режимы.

### 1.3. Ключевое правило для S-параметрического анализа в AWR

> **Импедансы всех портов должны быть согласованы с характеристическими импедансами линии.**
>
> Для CLIN с Z0O = 60 Ом: все порты должны иметь **Z = 60 Ом**.
>
> Для одиночной TLIN с Z0 = 120 Ом: оба порта должны иметь **Z = 120 Ом**.
>
> Порты с Z = 1 МОм, 12 кОм или любым значением, сильно отличающимся от Z₀ линии, дадут S-параметры с огромными потерями — это **артефакт нормировки**, а не реальные потери.

### 1.4. Соответствие элементов модели и реальной системы

| Реальный компонент RS-485 | Модель A (базовая) | Модель B (с драйвером) | Модель C (полная) |
|---------------------------|--------------------|------------------------|-------------------|
| Драйвер (выход) | PORT1, PORT2 (Z=60 Ом) | PORT1, PORT2 (Z=50 Ом) + RES 50 Ом | PORT1, PORT2 (Z=50 Ом) + RES 50 Ом |
| Кабель витой пары | CLIN | CLIN | CLIN |
| Терминатор 120 Ом | PORT3 + PORT4 (их суммарный дифф. импеданс = 120 Ом) | PORT3 + PORT4 (Z=60 Ом) | RES 120 Ом между A и B |
| Приёмник | PORT3 + PORT4 | PORT3 + PORT4 | RES 12 кОм + CAP 15 пФ на каждую линию |
| Паразитные параметры | Не учтены | Не учтены | CAP 15 пФ |

### 1.5. Допустимые упрощения

| Что упрощено | Как | Почему допустимо |
|--------------|-----|------------------|
| Потери в кабеле | CLIN — без потерь | Для кабеля ≤ 50 м на частотах ≤ 50 МГц потери < 2 дБ |
| Скрутка витой пары | Не моделируется | CLIN задаёт связь через импедансы мод — эквивалент скрутки |
| Драйвер (в модели A) | Порт с Z=60 Ом | Для базовой проверки линии достаточно |
| Приёмник (в моделях A, B) | Порт с Z=60 Ом | Порт поглощает сигнал как согласованная нагрузка |

---

## 2. Обязательные элементы модели

### Модель A (базовая) — минимальный набор

| № | Элемент AWR | Кол-во | Параметры | Назначение |
|---|-------------|--------|-----------|------------|
| 1 | **PORT** (P1) | 1 | PRT=1, Z=60 Ом | Вход линии A (драйвер +) |
| 2 | **PORT** (P2) | 1 | PRT=2, Z=60 Ом | Вход линии B (драйвер −) |
| 3 | **CLIN** | 1 | Z0E=90, Z0O=60, EL=60°, F0=0.005 ГГц | Витая пара |
| 4 | **PORT** (P3) | 1 | PRT=3, Z=60 Ом | Выход линии A (приёмник +) |
| 5 | **PORT** (P4) | 1 | PRT=4, Z=60 Ом | Выход линии B (приёмник −) |
| 6 | **GND** | 2 | — | Земля для CLIN |

**Всего: 6 элементов.** Это минимальная корректная модель.

### Модель B (с драйвером) — добавляем

| № | Элемент AWR | Параметры | Назначение |
|---|-------------|-----------|------------|
| 7 | **RES** (R_drv_A) | R=50 Ом | Выходное сопротивление драйвера, линия A |
| 8 | **RES** (R_drv_B) | R=50 Ом | Выходное сопротивление драйвера, линия B |

PORT1, PORT2 остаются с Z = 50 Ом (стандартный импеданс). PORT3, PORT4 — **по-прежнему Z = 60 Ом**.

### Параметры CLIN и их физический смысл

| Параметр | Значение | Что означает |
|----------|----------|--------------|
| **Z0E** | 90 Ом | Импеданс чётной моды (синфазный режим) |
| **Z0O** | 60 Ом | Импеданс нечётной моды (дифференциальный режим) |
| **EL** | 60° | Электрическая длина при частоте F0 |
| **F0** | 0.005 ГГц | Опорная частота (5 МГц) |

**Вычисляемые характеристики:**
- Zdiff = 2 × Z0O = 2 × 60 = **120 Ом** (стандарт RS-485)
- Zcomm = Z0E / 2 = 90 / 2 = **45 Ом**
- Коэффициент связи k = (Z0E − Z0O) / (Z0E + Z0O) = 30/150 = **0.2**
- Физическая длина ≈ EL × c / (360 × F0 × √εeff) ≈ 60 × 3e8 / (360 × 5e6 × 1.52) ≈ **6.6 м**
- Задержка: τ = EL / (360 × F0) = 60 / (360 × 5e6) = **33.3 нс**

---

## 3. Пошаговая инструкция: Модель A (базовая)

> **Предупреждение:** точные названия пунктов меню могут незначительно отличаться в зависимости от сборки AWR DE 17 (Cadence vs. NI). Логика действий сохраняется.

### Шаг 1. Создание проекта

1. Запустите AWR Design Environment 17.
2. **File → New Project** (или **Ctrl+N**).
3. **File → Save Project As...** → имя: `RS485_Model` → **Save**.

**Результат:** пустой проект открыт.

### Шаг 2. Создание схемы

1. В дереве проекта (Project Browser) найдите **Circuit Schematics**.
2. Правой кнопкой → **New Schematic** → имя: `RS485_Line_A` → **OK**.

**Результат:** пустой лист схемы.

### Шаг 3. Размещение CLIN (витая пара)

1. В Elements Browser: **Circuit Elements → Transmission Lines → Coupled Lines → CLIN**.
   - Или: **Draw → Add Element...** → поиск: `CLIN`.
2. Перетащите на центр схемы.
3. Дважды щёлкните по CLIN → **Element Options**:
   - **ZE** (Z0E): `90`
   - **ZO** (Z0O): `60`
   - **EL**: `60`
   - **F0**: `0.005`
4. **OK**.

**Расположение выводов CLIN:**
```
Pin 1 (вход A)  ────  CLIN  ──── Pin 3 (выход A)
Pin 2 (вход B)  ────  CLIN  ──── Pin 4 (выход B)
                      │  │
                    GND  GND
```

**Результат:** CLIN размещён с заданными параметрами.

### Шаг 4. Заземление CLIN

1. Добавьте 2 элемента **GND**: **Circuit Elements → Ports → GND** (или кнопка Ground на панели).
2. Подключите GND к обоим нижним (земляным) выводам CLIN.
   - Инструмент провод: **Draw → Wire** или клавиша **W**.

**Результат:** земляные выводы CLIN подключены.

### Шаг 5. Добавление PORT1 (вход линии A)

1. **Circuit Elements → Ports → PORT** → разместите слева от Pin 1 CLIN.
2. Дважды щёлкните → свойства:
   - **PRT:** `1`
   - **Z:** `60`
3. **OK**.
4. Проводом соедините PORT1 → Pin 1 CLIN.

### Шаг 6. Добавление PORT2 (вход линии B)

1. Добавьте PORT → разместите слева от Pin 2 CLIN.
2. Свойства: **PRT:** `2`, **Z:** `60`.
3. Соедините PORT2 → Pin 2 CLIN.

### Шаг 7. Добавление PORT3 (выход линии A)

1. Добавьте PORT → разместите справа от Pin 3 CLIN.
2. Свойства: **PRT:** `3`, **Z:** `60`.
3. Соедините Pin 3 CLIN → PORT3.

### Шаг 8. Добавление PORT4 (выход линии B)

1. Добавьте PORT → разместите справа от Pin 4 CLIN.
2. Свойства: **PRT:** `4`, **Z:** `60`.
3. Соедините Pin 4 CLIN → PORT4.

### Шаг 9. Проверка схемы

Итоговая схема модели A:

```
PORT1 (Z=60) ──── Pin1 ──┐              ┌── Pin3 ──── PORT3 (Z=60)
                          │     CLIN     │
PORT2 (Z=60) ──── Pin2 ──┘              └── Pin4 ──── PORT4 (Z=60)
                          │     │
                         GND   GND
```

Проверьте:
- 4 порта, все с Z = 60 Ом, номера: 1, 2, 3, 4 (все разные!).
- Все 4 сигнальных вывода CLIN подключены к портам.
- Оба земляных вывода CLIN подключены к GND.
- Нет висящих (красных) выводов.
- Нет лишних элементов.

### Шаг 10. Настройка частотного диапазона

1. Правой кнопкой по имени проекта в дереве → **Options** → вкладка **Frequencies**.
   Или: **Project → Project Options → Frequencies**.
2. Задайте:
   - **Start:** `0.001` ГГц (1 МГц)
   - **Stop:** `0.1` ГГц (100 МГц)
   - **Step:** `0.001` ГГц (1 МГц, итого 100 точек)
3. **Apply** → **OK**.

**Результат:** частотный диапазон 1–100 МГц задан.

---

## 4. Инструкция по моделированию (модель A)

### 4.1. Создание графиков

#### График 1: DB(S[3,1]) — передача от входа A к выходу A

1. В дереве проекта правой кнопкой по **Graphs** → **New Graph**.
2. **Name:** `S31_Transmission`, **Type:** `Rectangular` → **OK**.
3. Правой кнопкой внутри графика → **Add Measurement**:
   - **Measurement Type:** `Port Parameters` → `S`
   - **Measurement:** `S(3,1)` (или `S[3,1]`)
   - **Data Source:** `RS485_Line_A`
   - **Complex Modifier:** `DB`
4. **OK**.

#### График 2: DB(S[1,1]) — отражение от входа A

1. Новый график: `S11_Reflection`, `Rectangular`.
2. Add Measurement: `S(1,1)`, DB.

#### График 3: Ang(S[3,1]) — фаза передачи

1. Новый график: `S31_Phase`, `Rectangular`.
2. Add Measurement: `S(3,1)`, **Complex Modifier:** `Ang`.

#### График 4: DB(S[3,2]) — перекрёстная связь (crosstalk)

1. Новый график: `S32_Crosstalk`, `Rectangular`.
2. Add Measurement: `S(3,2)`, DB.

#### График 5: DB(S[4,2]) — передача по линии B

1. Новый график: `S42_Transmission_B`, `Rectangular`.
2. Add Measurement: `S(4,2)`, DB.

### 4.2. Запуск симуляции

Нажмите **F8** (или **Simulate → Analyze**, или зелёная стрелка ▶).

### 4.3. Ожидаемые результаты для модели A (все порты Z=60 Ом)

| Параметр | Ожидаемое значение | Что означает |
|----------|--------------------|--------------|
| **DB(S[3,1])** | **≈ 0 дБ** (от 0 до −0.5 дБ) на всех частотах | Сигнал полностью проходит от входа A к выходу A — линия согласована, потерь нет |
| **DB(S[1,1])** | **< −20 дБ** (идеально: −∞) | Отражений нет — входной порт согласован с линией |
| **Ang(S[3,1])** | Линейно убывает от 0° до −120° (при 100 МГц) | Сигнал задерживается в линии. Линейная фаза = постоянная задержка |
| **DB(S[4,2])** | **≈ 0 дБ** (совпадает с S[3,1]) | Линия B работает так же, как линия A (симметрия) |
| **DB(S[3,2])** | Значительный уровень (−10...−30 дБ), зависит от частоты | Связь между линиями — нормально для витой пары |

> **Критическая проверка:** если DB(S[3,1]) ≈ 0 дБ и DB(S[1,1]) < −20 дБ — **модель работает правильно**. Сигнал передаётся через линию без потерь и отражений.

> **Если DB(S[3,1]) = −77 дБ или ниже** — импедансы портов заданы неправильно. Проверьте, что **все 4 порта** имеют Z = 60 Ом. Не 50 Ом, не 120 Ом, не 1 МОм — именно 60 Ом.

---

## 5. Модель B: добавляем выходное сопротивление драйвера

После того как модель A даёт корректные результаты (S[3,1] ≈ 0 дБ), усложняем схему.

### 5.1. Что добавить

Добавляем два последовательных резистора R_drv на **входной** стороне CLIN:

```
PORT1 (Z=50) ── R_drv_A(50Ω) ── Pin1 ──┐              ┌── Pin3 ── PORT3 (Z=60)
                                         │     CLIN     │
PORT2 (Z=50) ── R_drv_B(50Ω) ── Pin2 ──┘              └── Pin4 ── PORT4 (Z=60)
                                         │     │
                                        GND   GND
```

### 5.2. Пошаговые изменения

1. Найдите **RES**: **Circuit Elements → Lumped Elements → Resistor → RES**.
2. Разместите R_drv_A **горизонтально** между PORT1 и Pin 1 CLIN:
   - R = `50` Ом.
   - Отсоедините провод PORT1→CLIN(1). Вставьте RES: PORT1 → RES → CLIN(1).
3. Аналогично R_drv_B между PORT2 и Pin 2 CLIN: R = `50` Ом.
4. Измените Z портов PORT1 и PORT2 на `50` Ом (вместо 60):
   - Двойной щелчок по PORT1 → Z = `50` → OK.
   - Двойной щелчок по PORT2 → Z = `50` → OK.
5. **PORT3 и PORT4 оставьте Z = 60 Ом** — они по-прежнему моделируют согласованную нагрузку.

### 5.3. Запуск и ожидаемые результаты модели B

Нажмите **F8**.

| Параметр | Ожидаемое значение | Почему |
|----------|--------------------|--------|
| **DB(S[3,1])** | **−3 ... −6 дБ** (примерно постоянно до 10 МГц) | Потери из-за делителя напряжения: R_drv (50 Ом) + PORT (50 Ом) = 100 Ом источника, а линия 60 Ом. Часть мощности отражается на входе |
| **DB(S[1,1])** | **−5 ... −10 дБ** | Входное рассогласование: источник 100 Ом, линия 60 Ом → Γ = (100−60)/(100+60) = 0.25 → −12 дБ |
| **Ang(S[3,1])** | Линейно убывает | Задержка сохраняется |
| **DB(S[4,2])** | Совпадает с S[3,1] | Симметрия |
| **DB(S[3,2])** | Значительный уровень | Связь между линиями |

> **Критическая проверка:** S[3,1] должен быть в диапазоне **−3 ... −8 дБ**, а не −77 дБ. Если значения снова аномально низкие — проверьте, что PORT3 и PORT4 имеют Z = **60 Ом**, а не 1 МОм.

> **Потери −3...−6 дБ — это нормально.** Они обусловлены рассогласованием выходного сопротивления драйвера (100 Ом дифференциально) с линией (120 Ом дифференциально). В реальной системе RS-485 эти потери не критичны: напряжение драйвера ±5 В, а порог приёмника ±200 мВ — запас по уровню огромный.

---

## 6. Эксперименты для проверки модели

### 6.1. Эксперимент 1: изменение длины линии

Исходная модель A (все порты 60 Ом, без R_drv).

1. Измените EL в CLIN: `60` → `180` → `360` (соответствует ~6.6 м → ~20 м → ~40 м).
2. Запускайте F8 после каждого изменения.

**Ожидаемый результат:**
- DB(S[3,1]) **не меняется** (≈ 0 дБ) — линия без потерь, согласована.
- Ang(S[3,1]) **растёт** пропорционально длине — задержка увеличивается.
- При EL=180° фаза при 5 МГц = −180°. При EL=360° фаза при 5 МГц = −360°.

**Что это доказывает:** линия передаёт сигнал без потерь при согласовании, независимо от длины. Задержка пропорциональна длине. Модель физически корректна.

### 6.2. Эксперимент 2: рассогласование на выходе (имитация отсутствия терминатора)

Исходная модель A.

1. Измените Z порта PORT3 с `60` на `6000` Ом.
2. Измените Z порта PORT4 с `60` на `6000` Ом.
3. Запустите F8.

**Ожидаемый результат:**
- DB(S[1,1]) **резко возрастает** (ближе к 0 дБ) — появляются периодические пики и провалы (стоячие волны).
- DB(S[3,1]) **приобретает осциллирующий характер** — неравномерная передача.
- Периодичность пиков: ΔF ≈ F0 × 360/EL = 5 × 360/60 = 30 МГц.

**Что это доказывает:** без согласующего терминатора (когда Z_нагрузки >> Z₀ линии) возникают стоячие волны и отражения. Это главная проблема RS-485 без терминатора.

> **Примечание:** S-параметры нормируются к Z порта, поэтому S[3,1] при Z₃=6000 Ом будет содержать нормировочное смещение. Но **качественная картина** (осцилляции, пики S[1,1]) будет абсолютно корректной и наглядной.

4. **Верните** Z PORT3 и PORT4 обратно на `60` Ом.

### 6.3. Эксперимент 3: влияние связи между линиями

Исходная модель A.

1. Измените Z0E с `90` на `60` (сделайте Z0E = Z0O).
2. Запустите F8.

**Ожидаемый результат:**
- DB(S[3,2]) **резко падает** (→ −∞ дБ) — связь между линиями исчезает.
- DB(S[3,1]) и DB(S[4,2]) не меняются (≈ 0 дБ).
- Коэффициент связи k = (Z0E−Z0O)/(Z0E+Z0O) = 0 — линии стали независимыми.

**Что это доказывает:** Z0E ≠ Z0O задаёт электромагнитную связь между проводниками витой пары. При Z0E = Z0O связь исчезает — линии становятся двумя независимыми проводниками.

3. **Верните** Z0E = `90`.

### 6.4. Эксперимент 4: влияние R_drv (модель B)

Сравните модель A и модель B (с R_drv):

| Параметр | Модель A (без R_drv) | Модель B (R_drv = 50 Ом) |
|----------|----------------------|---------------------------|
| DB(S[3,1]) | ≈ 0 дБ | ≈ −3...−6 дБ |
| DB(S[1,1]) | < −20 дБ | −5...−10 дБ |

**Что это доказывает:** выходное сопротивление драйвера (R_drv) создаёт потери из-за рассогласования. Это реалистично: в реальной системе RS-485 драйвер не идеален.

### 6.5. Эксперимент 5: неправильный дифференциальный импеданс

Исходная модель B (с R_drv).

1. Измените Z0O с `60` на `30` (→ Zdiff = 60 Ом вместо 120 Ом). Это имитирует подключение «неправильного» кабеля.
2. Запустите F8.

**Ожидаемый результат:**
- DB(S[1,1]) **значительно ухудшается** — рассогласование между источником и линией выросло.
- DB(S[3,1]) **снижается** — больше мощности отражается.
- Если EL велико — появляются периодические осцилляции.

3. **Верните** Z0O = `60`.

---

## 7. Настройка смешанных (Mixed-Mode) S-параметров

### 7.1. Что это и зачем

Mixed-mode S-параметры разделяют дифференциальный и синфазный режимы:

| Параметр | Что показывает |
|----------|----------------|
| **Sdd11** | Отражение дифференциального сигнала на входе |
| **Sdd21** | Передача дифференциального сигнала (вход → выход) |
| **Scc11** | Отражение синфазной помехи на входе |
| **Scc21** | Передача синфазной помехи |
| **Sdc21** | Конверсия: дифференциальный → синфазный |
| **Scd21** | Конверсия: синфазный → дифференциальный |

### 7.2. Как настроить в AWR DE 17

1. В дереве проекта: правой кнопкой по схеме `RS485_Line_A` → **Options** (или **Properties**).
2. Найдите вкладку **Port** (или **Port Grouping**, **Balanced Ports**, **Port Setup**).
3. Определите **две балансные пары**:
   - **Пара 1 (вход):** PORT1 (+), PORT2 (−)
   - **Пара 2 (выход):** PORT3 (+), PORT4 (−)
4. **Apply** → **OK**.
5. Создайте новый график → Add Measurement → ищите **Sdd**, **Scc**, **Sdc**, **Scd** в разделе `Port Parameters`.

> **Если mixed-mode параметры недоступны** в вашей версии AWR или не получается их настроить — это **не критично**. Обычные S[3,1], S[1,1] и т.д. уже дают полную информацию о работе линии. Mixed-mode — дополнительная возможность для расширенного анализа.

### 7.3. Ожидаемые mixed-mode результаты (модель A)

| Параметр | Значение | Комментарий |
|----------|----------|-------------|
| Sdd11 | < −20 дБ | Дифференциальное отражение — малое (согласовано) |
| Sdd21 | ≈ 0 дБ | Дифференциальный сигнал проходит полностью |
| Scc11 | > −20 дБ, зависит от частоты | Синфазное отражение — хуже, т.к. Z0E ≠ порт. Z |
| Scc21 | < Sdd21 | Синфазный сигнал проходит хуже |

---

## 8. Модель C: полная эквивалентная схема RS-485 (2-портовая)

Эта модель добавляет явный терминатор, приёмник и паразитную ёмкость. Но **PORT3 и PORT4 убираются** — выходная сторона не имеет портов, только пассивные нагрузки. Модель становится **2-портовой**: PORT1 и PORT2 на входе.

### 8.1. Схема

```
PORT1 (Z=50) ── R_drv_A(50Ω) ── Pin1 ──┐              ┌── Pin3 ─┬─ R_rcv_A(12kΩ) ── GND
                                         │     CLIN     │         ├─ C_rcv_A(15пФ) ── GND
                                         │              │    R_term(120Ω)
                                         │              │         │
PORT2 (Z=50) ── R_drv_B(50Ω) ── Pin2 ──┘              └── Pin4 ─┼─ R_rcv_B(12kΩ) ── GND
                                         │     │                  └─ C_rcv_B(15пФ) ── GND
                                        GND   GND
```

### 8.2. Что измеряем

В 2-портовой модели доступны:

| Параметр | Что показывает |
|----------|----------------|
| S[1,1] | Отражение от входа линии A (включая все нагрузки на выходе) |
| S[2,2] | Отражение от входа линии B |
| S[2,1] | Связь между PORT1 и PORT2 через CLIN (ближний конец) |
| S[1,2] | Связь в обратном направлении |

> **Ограничение:** в 2-портовой модели **нельзя** напрямую измерить S[3,1] (передачу на дальний конец), потому что портов 3 и 4 нет. Зато S[1,1] точно покажет, согласована ли линия, есть ли стоячие волны.

### 8.3. Ожидаемые результаты модели C

| Параметр | С R_term = 120 Ом | Без R_term (удалить) |
|----------|--------------------|----------------------|
| DB(S[1,1]) | **< −10 дБ**, слабая зависимость от частоты | Периодические осцилляции, пики до ~0 дБ |
| DB(S[2,2]) | Аналогично S[1,1] | Аналогично |
| DB(S[2,1]) | Связь через CLIN | Связь + стоячие волны |

### 8.4. Как собрать (пошагово)

1. **Начните с модели B** (PORT1, PORT2, R_drv_A, R_drv_B, CLIN, PORT3, PORT4).
2. **Удалите PORT3 и PORT4** (выделить → Delete).
3. Добавьте **RES** (R_term, 120 Ом) **вертикально** между Pin 3 и Pin 4 CLIN:
   - Верхний вывод R_term → тот же узел, что Pin 3 CLIN.
   - Нижний вывод R_term → тот же узел, что Pin 4 CLIN.
4. Добавьте **RES** (R_rcv_A, 12000 Ом) от узла Pin 3 к **GND**.
5. Добавьте **CAP** (C_rcv_A, 15 пФ) параллельно R_rcv_A (от узла Pin 3 к GND).
   - Путь: **Circuit Elements → Lumped Elements → Capacitor → CAP**.
6. Добавьте **RES** (R_rcv_B, 12000 Ом) от узла Pin 4 к **GND**.
7. Добавьте **CAP** (C_rcv_B, 15 пФ) параллельно R_rcv_B (от узла Pin 4 к GND).
8. Подключите все GND.
9. Запустите F8.

---

## 9. Полная методика проверки работоспособности

### 9.1. Пошаговая проверка

| Шаг | Что проверить | Ожидание | Если не выполняется |
|-----|---------------|----------|---------------------|
| 1 | Модель A: DB(S[3,1]) | ≈ 0 дБ (от 0 до −1 дБ) | Проверить Z всех портов = 60 Ом |
| 2 | Модель A: DB(S[1,1]) | < −20 дБ | Проверить Z0O = 60 и Z портов = 60 |
| 3 | Модель A: S[3,1] = S[4,2] | Совпадают | Проверить симметрию параметров |
| 4 | Модель A: Ang(S[3,1]) | Линейно убывает | Нормальная задержка |
| 5 | Модель B: DB(S[3,1]) | −3...−6 дБ | R_drv создаёт ожидаемые потери |
| 6 | Модель B: DB(S[1,1]) | −5...−12 дБ | Рассогласование из-за R_drv — ожидаемо |
| 7 | Эксп. 2: PORT3/4 Z→6000 | S[1,1] осциллирует | Стоячие волны — терминатор нужен |
| 8 | Модель C: DB(S[1,1]) с R_term | < −10 дБ | Линия согласована терминатором |
| 9 | Модель C: DB(S[1,1]) без R_term | Осцилляции | Стоячие волны подтверждены |

### 9.2. Признаки правильно работающей модели

1. **S[3,1] ≈ 0 дБ** (модель A) — сигнал проходит полностью, линия без потерь.
2. **S[1,1] < −20 дБ** (модель A) — отражений нет, согласование идеальное.
3. **S[3,1] = S[4,2]** — обе линии витой пары симметричны.
4. **Ang(S[3,1]) линейно убывает** — постоянная задержка, нет дисперсии.
5. **При увеличении EL** фаза растёт, но амплитуда S[3,1] не меняется.
6. **При рассогласовании** (Z порта ≠ Z0O) появляются осцилляции — модель чувствительна к согласованию.
7. **S[3,2] ≠ 0** (есть связь между линиями) — CLIN работает как связанная линия.

### 9.3. Признаки неправильной модели

| Симптом | Диагноз | Исправление |
|---------|---------|-------------|
| S[3,1] = −70...−120 дБ | Z портов 3/4 = 1 МОм или другое высокое значение | Установить Z = 60 Ом |
| S[3,1] = −∞ (нет сигнала) | Разрыв провода или незаземлённый CLIN | Проверить все соединения и GND |
| S[3,1] > +1 дБ | Усиление в пассивной цепи — невозможно | Проверить параметры элементов |
| S[1,1] = 0 дБ всюду | Полное отражение: CLIN не подключён или EL=0 | Проверить EL > 0, все провода |
| S[3,1] не зависит от частоты | EL = 0 (провод, а не линия) | Задать EL = 60° |
| S[3,1] ≠ S[4,2] (большая разница) | Несимметрия параметров | Проверить: Z0E, Z0O, Z портов |
| «Floating node» | Неподключённый вывод | Заземлить все земляные выводы CLIN |
| «Duplicate port number» | Два порта с одинаковым PRT | PRT: 1, 2, 3, 4 — все разные |
| Group Delay = 0 | EL = 0 или F0 не задана | F0 = 0.005 ГГц, EL = 60° |
| DB(Ang(S[3,1])) = 220...225 | Построен DB от угла — бессмысленная величина | Нужно строить Ang(S[3,1]) без DB, или DB(S[3,1]) без Ang |

---

## 10. Обязательные результаты для учебной работы

### 10.1. Список графиков для сохранения

| № | График | Модель | Зачем |
|---|--------|--------|-------|
| 1 | DB(S[3,1]), все порты 60 Ом | A | Доказать: сигнал проходит через линию (≈ 0 дБ) |
| 2 | DB(S[1,1]), все порты 60 Ом | A | Доказать: отражений нет (< −20 дБ) |
| 3 | DB(S[3,1]) с R_drv | B | Показать влияние импеданса драйвера |
| 4 | DB(S[1,1]) при Z PORT3/4 = 6000 Ом | A (эксп. 2) | Показать стоячие волны без терминатора |
| 5 | Ang(S[3,1]) | A | Подтвердить задержку в линии |
| 6 | DB(S[3,2]) | A | Показать связь между линиями |
| 7 | DB(S[1,1]) модели C с R_term | C | Согласованная нагрузка (полная модель) |
| 8 | DB(S[1,1]) модели C без R_term | C | Несогласованная нагрузка (стоячие волны) |

### 10.2. Как сохранить графики

- Правой кнопкой по графику → **Copy to Clipboard** → вставить в Word/отчёт.
- Или: **File → Export** → выбрать формат (PNG, EMF, PDF).
- Или: **Print** с выводом в PDF.

### 10.3. Интерпретация для отчёта

#### Вывод 1 (модель A):

> При согласованных портах (Z = 60 Ом = Z0O) коэффициент передачи DB(S[3,1]) составляет ≈ 0 дБ на всех частотах до 100 МГц, а коэффициент отражения DB(S[1,1]) < −20 дБ. Это подтверждает, что модель связанной линии передачи CLIN корректно работает: дифференциальный сигнал передаётся от входа к выходу без потерь при условии согласования импедансов.

#### Вывод 2 (модель B):

> Добавление выходного сопротивления драйвера (R_drv = 50 Ом, суммарный дифференциальный импеданс 100 Ом) приводит к потерям передачи 3–6 дБ из-за рассогласования с линией (Zdiff = 120 Ом). Это реалистично отражает поведение реального драйвера RS-485: часть мощности отражается от входа линии. Для RS-485 это не критично благодаря большому запасу по уровню сигнала.

#### Вывод 3 (эксперимент с рассогласованием):

> При увеличении импеданса нагрузки до 6000 Ом (имитация отсутствия терминатора) коэффициент отражения S[1,1] резко возрастает и приобретает периодическую частотную зависимость, характерную для стоячих волн. Это наглядно демонстрирует необходимость терминирующего резистора 120 Ом для подавления отражений в линии RS-485.

#### Вывод 4 (модель C):

> В полной 2-портовой модели с явным терминатором 120 Ом, входным сопротивлением приёмника 12 кОм и паразитной ёмкостью 15 пФ коэффициент отражения S[1,1] < −10 дБ в рабочей полосе частот. При удалении терминатора отражение возрастает, подтверждая его критическую роль.

---

## 11. Типичные ошибки

### 11.1. Каталог ошибок

#### 1. PORT3/PORT4 с Z = 1 МОм (самая частая ошибка!)

| | |
|---|---|
| **Проявление** | DB(S[3,1]) = −70...−120 дБ, что выглядит как «сигнал не проходит» |
| **Причина** | S-параметры нормируются к Z порта. При Z = 1 МОм нормировка даёт ≈ −43 дБ только за счёт √(Z₁/Z₃) |
| **Как заметить** | Значения S[3,1] на десятки дБ ниже ожидаемых |
| **Исправление** | **Z PORT3 = Z PORT4 = 60 Ом** |

#### 2. DB(Ang(S[...])) — логарифм от фазы

| | |
|---|---|
| **Проявление** | График показывает значения 200–230 — бессмысленные числа |
| **Причина** | Выбрана комбинация DB + Ang — это 20·log₁₀(фаза в градусах). Фаза 100°...170° → DB = 40...45. Не имеет физического смысла |
| **Как заметить** | Значения не соответствуют ожидаемым дБ и не похожи на углы |
| **Исправление** | Для амплитуды: `DB(S[...])` (без Ang). Для фазы: `Ang(S[...])` (без DB). **Никогда** не используйте DB и Ang одновременно |

#### 3. F0 задана в МГц вместо ГГц

| | |
|---|---|
| **Проявление** | Линия ведёт себя как в 1000 раз короче; задержка ≈ 0 |
| **Как заметить** | Group Delay ≈ 0, фаза S[3,1] почти не меняется |
| **Исправление** | F0 = `0.005` ГГц (не 5!) |

#### 4. EL = 0

| | |
|---|---|
| **Проявление** | CLIN превращается в провод нулевой длины |
| **Как заметить** | S[3,1] = 0 дБ на всех частотах, фаза = 0 |
| **Исправление** | EL = `60` (или другое значение > 0) |

#### 5. Земляные выводы CLIN не заземлены

| | |
|---|---|
| **Проявление** | Ошибка «Floating node» или абсурдные результаты |
| **Как заметить** | Красные маркеры на схеме, ошибка при запуске F8 |
| **Исправление** | Подключить оба нижних вывода CLIN к GND |

#### 6. Одинаковые номера портов

| | |
|---|---|
| **Проявление** | Ошибка симуляции |
| **Как заметить** | Сообщение об ошибке в Output Window |
| **Исправление** | PRT: 1, 2, 3, 4 — все разные |

#### 7. Z0E = Z0O (нет связи между линиями)

| | |
|---|---|
| **Проявление** | S[3,2] = −∞ дБ; линии независимы |
| **Как заметить** | Нет перекрёстной связи |
| **Исправление** | Z0E > Z0O. Для RS-485: Z0E = 90, Z0O = 60 |

#### 8. Терминатор подключён к GND вместо между линиями

| | |
|---|---|
| **Проявление** | В модели C: S[1,1] не улучшается при добавлении терминатора |
| **Как заметить** | Картина с/без терминатора почти одинакова |
| **Исправление** | R_term подключить **между** Pin 3 и Pin 4 CLIN (между A и B) |

#### 9. Неверный тип элемента (TLIN вместо CLIN)

| | |
|---|---|
| **Проявление** | Только 2 сигнальных вывода; нет двух линий |
| **Как заметить** | Нельзя подключить 4 порта; нет S[3,1] или S[4,2] |
| **Исправление** | Использовать CLIN (4-портовый), а не TLIN (2-портовый) |

#### 10. Единицы ёмкости перепутаны

| | |
|---|---|
| **Проявление** | C_rcv = 15 Ф (вместо 15 пФ) — КЗ на высоких частотах |
| **Как заметить** | S[3,1] резко падает при малых частотах |
| **Исправление** | Проверить единицы: `15` пФ = `1.5e-11` Ф. В AWR по умолчанию пФ |

---

## 12. Печатная плата / топология

### 12.1. Витая пара ≠ дифференциальная пара на плате

| Характеристика | Витая пара (кабель RS-485) | Дифференциальная пара на плате |
|----------------|----------------------------|--------------------------------|
| Среда | Изолированные проводники в кабеле | Медные дорожки на FR-4 |
| Длина | 1–1200 м | 1–50 см |
| Zdiff | 120 Ом (стандарт) | Зависит от W, S, H (обычно 90–120 Ом) |
| Модель AWR | **CLIN** (идеальные связанные линии) | **MCLIN** + **MSUB** (микрополоски на подложке) |

### 12.2. Что можно сделать в AWR

AWR DE 17 **позволяет:** моделировать участок дифференциальной пары на плате (MCLIN), увидеть Layout (топологию дорожек), рассчитать W и S через TXLine.

AWR DE 17 **не позволяет:** создать полноценную PCB, генерировать Gerber, разместить компоненты.

### 12.3. Пошаговая инструкция: участок платы (MCLIN)

1. Создайте новую схему: `RS485_PCB`.
2. Добавьте **MSUB** (Microstrip Substrate): **Circuit Elements → Substrates → MSUB**.
   - Er = `4.6`, H = `1.5` мм, T = `0.035` мм, Rho = `1`, Tand = `0.02`.
3. Рассчитайте W и S: **Tools → TXLine** → Coupled Microstrip → Zdiff = 120 Ом → **Synthesize**.
4. Добавьте **MCLIN**: **Circuit Elements → Transmission Lines → Coupled Lines → MCLIN**.
   - W = значение из TXLine (≈ 0.3 мм), S = значение из TXLine (≈ 0.2 мм), L = `30` мм.
5. Подключите 4 порта (все Z = 60 Ом), заземлите.
6. Посмотрите топологию: **View → Layout** — увидите две параллельные дорожки.

### 12.4. Как объяснить в учебной работе

> Модель RS-485 состоит из двух частей:
> 1. **Кабель витой пары** — элемент CLIN (идеальные связанные линии), Z0E = 90 Ом, Z0O = 60 Ом, длина ~6.6 м.
> 2. **Участок печатной платы** — элемент MCLIN (связанные микрополоски) на подложке FR-4 (MSUB), длина 30 мм.
>
> Полноценная печатная плата в AWR Design Environment не создаётся — для этого используется специализированный САПР (Altium, KiCad). AWR используется для расчёта импедансов и электромагнитного моделирования критических участков.

---

## 13. Итоговый чек-лист

### Модель A (базовая) — обязательно сделать первой

- [ ] CLIN: Z0E=90, Z0O=60, EL=60, F0=0.005.
- [ ] Земляные выводы CLIN → GND (2 шт.).
- [ ] PORT1: PRT=1, **Z=60**. PORT2: PRT=2, **Z=60**.
- [ ] PORT3: PRT=3, **Z=60**. PORT4: PRT=4, **Z=60**.
- [ ] Все номера портов разные.
- [ ] Нет висящих выводов.
- [ ] Частоты: 0.001–0.1 ГГц, шаг 0.001.
- [ ] График DB(S[3,1]) создан.
- [ ] **F8** → DB(S[3,1]) ≈ **0 дБ**? Если да — модель работает!
- [ ] DB(S[1,1]) < −20 дБ? Если да — согласование в порядке.

### Модель B (с R_drv) — после успеха модели A

- [ ] R_drv_A = R_drv_B = 50 Ом, между портами и CLIN.
- [ ] PORT1, PORT2: Z = 50 Ом (изменены!).
- [ ] PORT3, PORT4: Z = **60 Ом** (не менять!).
- [ ] F8 → DB(S[3,1]) ≈ −3...−6 дБ.

### Эксперименты — для отчёта

- [ ] Без терминатора (Z PORT3/4 → 6000): осцилляции S[1,1].
- [ ] Разная длина (EL=60, 180, 360): фаза растёт, амплитуда нет.
- [ ] Нет связи (Z0E=Z0O=60): S[3,2] → −∞.

### Для отчёта

- [ ] Сохранены ≥ 6 графиков.
- [ ] Описана эквивалентная схема.
- [ ] Указаны параметры: Z0E, Z0O, EL, F0, Zdiff.
- [ ] Написаны выводы по каждому эксперименту.

---

## Приложение А. Справочные формулы

| Параметр | Формула | Значение для базовой модели |
|----------|---------|------------------------------|
| Дифф. импеданс | Zdiff = 2 × Z0O | 2 × 60 = **120 Ом** |
| Синфазный импеданс | Zcomm = Z0E / 2 | 90 / 2 = **45 Ом** |
| Коэффициент связи | k = (Z0E − Z0O)/(Z0E + Z0O) | 30/150 = **0.2** |
| Физическая длина | L = EL × c/(360 × F0 × √εeff) | ≈ **6.6 м** |
| Задержка | τ = EL/(360 × F0) | **33.3 нс** |
| Коэффициент отражения | Γ = (Z_L − Z0)/(Z_L + Z0) | 0 при Z_L = Z0 |
| КСВН | VSWR = (1 + |Γ|)/(1 − |Γ|) | 1.0 при Γ = 0 |

## Приложение Б. Параметры RS-485

| Параметр | Значение |
|----------|----------|
| Дифференциальный импеданс кабеля | 120 ± 10 Ом |
| Терминирующий резистор | 120 Ом |
| Максимальная скорость | 10 Мбит/с |
| Максимальная длина | 1200 м |
| Выходное напряжение драйвера | ±1.5 ... ±6 В (дифф.) |
| Порог чувствительности приёмника | ±200 мВ |
| Входное сопротивление приёмника | ≥ 12 кОм |
| Выходное сопротивление драйвера | 10–60 Ом |
| Входная ёмкость приёмника | 10–30 пФ |

## Приложение В. Горячие клавиши AWR DE 17

| Действие | Клавиша |
|----------|---------|
| Симуляция | **F8** |
| Провод | **W** |
| Увеличить | **Ctrl + колесо** |
| Уместить в окно | **Home** |
| Повернуть | **Ctrl+R** |
| Свойства | **Двойной щелчок** |
| Удалить | **Delete** |
| Отмена | **Ctrl+Z** |

## Приложение Г. Выводы CLIN

```
           CLIN
           ┌────────────────┐
Pin 1 ────►│  Линия A (+)   ├────► Pin 3
           │                │
Pin 2 ────►│  Линия B (−)   ├────► Pin 4
           └───┬────────┬───┘
               │        │
             GND_1    GND_2
```

- Pin 1 → вход A → PORT1 (или через R_drv_A)
- Pin 2 → вход B → PORT2 (или через R_drv_B)
- Pin 3 → выход A → PORT3 (Z=60!)
- Pin 4 → выход B → PORT4 (Z=60!)
- GND_1, GND_2 → земля
