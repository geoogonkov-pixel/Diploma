# Следующие шаги: от схемы к результатам

## Где вы сейчас

Схема собрана, EM Structure создана, графики заведены, ошибок 0. Всё готово для запуска.

---

## Шаг 1. Проверьте частотный диапазон

1. В дереве проекта → правой кнопкой по **Project** (самый верх дерева) → **Options** → вкладка **Frequencies**.
2. Убедитесь, что задано:
   - **Start:** `0.001` GHz (= 1 МГц)
   - **Stop:** `0.1` GHz (= 100 МГц)
   - **Step:** `0.001` GHz (= 1 МГц)
3. Если частоты не заданы или стоят по умолчанию — введите эти значения → **Apply** → **OK**.

Без этого графики будут пустыми.

---

## Шаг 2. Запустите circuit-level симуляцию

Нажмите **F8** (или Simulate → Analyze).

AWR рассчитает S-параметры схемы. Это мгновенно (< 1 секунды).

---

## Шаг 3. Проверьте графики

Откройте каждый график (щёлкните по вкладке вверху). Вот что ожидать:

### Graph 1: DB(S[4,2]) и DB(S[3,1]) — коэффициенты передачи

**Что ожидать при PORT3/PORT4 = 12000 Ом:**

S[3,1] и S[4,2] будут показывать примерно **−20...−30 дБ**. Это не означает, что сигнал теряется! Это артефакт нормировки — S-параметры нормируются к Z порта, и при Z₃ = 12 кОм vs Z₁ = 60 Ом возникает множитель √(60/12000) ≈ 0.07 → −23 дБ.

Для корректной интерпретации передачи через линию лучше смотреть на **S[1,1]** (отражение) — он не зависит от этого артефакта.

### Graph 6: DB(S[1,1]) — коэффициент отражения (return loss)

**Это главный результат.** Ожидание:
- DB(S[1,1]) **< −10 дБ** в полосе 1–100 МГц → линия согласована, терминатор R3=120 Ом работает.
- Если S[1,1] ≈ 0 дБ — полное отражение, что-то не так.
- Если S[1,1] осциллирует — стоячие волны (проверить нагрузку).

### Graph 2, 3: Ang(S[...]) — фазы

- Фаза должна **линейно убывать** с частотой — это нормальная задержка в линии.
- Нелинейная фаза → дисперсия (возможно проблема в модели).

### Graph 5: DB(MagMe...) — если это Magnitude

Зависит от того, что именно измеряется. Посмотрите, что написано в свойствах графика.

---

## Шаг 4. Сохраните скриншоты графиков

Для каждого графика:
1. Щёлкните по вкладке графика, чтобы он был активен.
2. Правой кнопкой внутри графика → **Copy to Clipboard**.
3. Вставьте в Word / Paint / отчёт (Ctrl+V).

Или: **File → Export** → PNG/EMF.

---

## Шаг 5. Запуск EM-симуляции (AXIEM)

Теперь — главное: переход к электромагнитной модели для получения распределения поля.

### Способ A: через EXTRACT (у вас уже настроен)

EXTRACT-блок в вашей схеме автоматически извлечёт геометрию MCLIN и передаст её в AXIEM.

1. EXTRACT уже должен был запуститься при F8. Проверьте:
   - В дереве проекта → **EM Structures** → должна появиться структура `EM_Extract_Doc` (или `EM_Extract`).
   - Если она есть — AXIEM уже рассчитал S-параметры EM-модели.

2. Если EM Structure не появилась:
   - Убедитесь, что в EXTRACT стоит **Override_Options = Yes**.
   - Убедитесь, что **STACKUP** задан в параметрах EXTRACT (STACKUP=SUB2 или пустой, если используется MSUB).
   - Нажмите F8 снова.

### Способ B: создать EM Structure вручную (если EXTRACT не сработал)

1. Правой кнопкой по **EM Structures** → **New EM Structure**.
2. Name: `RS485_Manual`, Simulator: **AXIEM** (или **Analyst** если доступен).
3. OK.
4. Настройте Stackup:
   - Слой 1 (снизу): Ground Plane (PEC)
   - Слой 2: FR-4 (Er=4.6, H=1.5 мм, Tand=0.02)
   - Слой 3 (сверху): Metal (Cu, T=0.035 мм)
5. Нарисуйте 2 прямоугольника на верхнем слое:
   - Проводник A: W=0.4 мм, L=30 мм
   - Проводник B: W=0.4 мм, L=30 мм, зазор S=0.3 мм
6. Добавьте 4 Edge Port на торцах.
7. Задайте частоты: 0.005–0.1 GHz, шаг 0.005.
8. F8 — запуск EM-симуляции.

---

## Шаг 6. Визуализация результатов EM-модели

### Если AXIEM — распределение тока

1. В дереве проекта → **EM Structures** → правой кнопкой по вашей EM Structure.
2. **Animate** или **View Current Density**.
3. Вы увидите цветовую карту тока на проводниках:
   - Максимум тока на **внутренних краях** дорожек (обращённых друг к другу) — это дифференциальный режим.
   - Это важный результат для отчёта.

### Если Analyst доступен — распределение E-field

1. Если при создании EM Structure можно выбрать **Analyst** вместо AXIEM:
   - Создайте новую EM Structure с Analyst.
   - Повторите геометрию (или скопируйте).
   - Запустите F8.
2. После расчёта → правой кнопкой по EM Structure → **View 3D** / **View Fields**.
3. Выберите: **E-Field → Magnitude**.
4. Выберите частоту: **5 МГц** (0.005 GHz).
5. Постройте **поперечное сечение (Cross-Section)**: плоскость XZ на Y = 15 мм (середина линии).
6. Сохраните скриншот — это **главный результат** (распределение E-field = аналог распределения потенциала).

### Если Analyst НЕ доступен

Используйте AXIEM для S-параметров и Current Density. Распределение E-field опишите аналитически:
> *Электрическое поле сконцентрировано между проводниками дифференциальной пары (область максимального градиента потенциала) и в диэлектрике между проводниками и земляной плоскостью.*

---

## Шаг 7. Получение S-параметров из EM-модели

После EM-симуляции добавьте графики с данными из EM Structure:

1. В любом графике → правой кнопкой → **Add Measurement**.
2. **Data Source:** выберите имя вашей EM Structure (не Schematic!).
3. Добавьте: S(3,1) DB, S(1,1) DB.
4. Сравните с результатами circuit-level модели — они должны быть **близки**, но EM-модель покажет небольшие дополнительные потери (из-за Tand FR-4 и эффектов краёв).

---

## Шаг 8. Эксперименты для отчёта

После того как базовая модель работает, проведите эксперименты:

### Эксперимент 1: Без терминатора
1. Удалите R3 (120 Ом) или отсоедините его.
2. F8.
3. S[1,1] должен **резко ухудшиться** (осцилляции, пики к 0 дБ) — стоячие волны.
4. Скриншот → вывод: «терминатор необходим».
5. **Верните R3 обратно.**

### Эксперимент 2: Изменение зазора S
1. В MCLIN измените S с 0.3 мм на 1.0 мм.
2. F8.
3. Crosstalk (S[3,2]) должен уменьшиться, Zdiff — вырасти.
4. В EM-модели: E-field между проводниками ослабеет.
5. **Верните S = 0.3 мм.**

### Эксперимент 3: Изменение ширины W
1. В MCLIN измените W с 0.4 мм на 1.0 мм.
2. F8.
3. Zdiff уменьшится, поле распределится шире.
4. **Верните W = 0.4 мм.**

---

## Краткая последовательность действий

```
1. Project Options → Frequencies: 0.001–0.1 GHz, шаг 0.001
2. F8 → circuit-level S-параметры
3. Проверить графики: S[1,1] < −10 дБ? Фаза линейна?
4. EM Structures → проверить, что EM_Extract создана
5. Открыть EM Structure → View Current Density (AXIEM)
6. Или: создать EM Structure с Analyst → F8 → View Fields → E-field
7. Сохранить скриншоты: графики + поле/ток
8. Эксперименты: без R3, разный S, разный W
9. Сохранить всё для отчёта
```

---

## Замечание о PORT3/PORT4 (Z=12000 Ом)

Ваша текущая схема с PORT3/PORT4 = 12 кОм **будет работать** — симуляция запустится и даст результаты. Но значения S[3,1] будут заниженными (~−20 дБ) из-за нормировки к 12 кОм.

Для отчёта это можно интерпретировать так:
> *S[3,1] показывает передачу мощности от 60-Ом источника к 12-кОм нагрузке приёмника. Низкое значение (−20 дБ) обусловлено высоким входным сопротивлением приёмника RS-485, который потребляет минимальную мощность из линии. Основная мощность поглощается терминатором (120 Ом).*

Это физически корректное объяснение.

Главный показатель работоспособности — **S[1,1]** (отражение): если < −10 дБ, линия согласована и работает.
