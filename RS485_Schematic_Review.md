# Разбор вашей схемы RS-485 в AWR DE 17

## Что вы собрали (по скриншоту)

Вы построили полную модель RS-485 с MCLIN, драйвером, терминатором, приёмником, MSUB, STACKUP и EXTRACT-блоком для AXIEM. Это правильный подход. Однако в модели есть **4 критические ошибки**, из-за которых результаты симуляции будут некорректными. Ниже — полный разбор каждого элемента.

---

## Элементы вашей схемы: что правильно, что нет

### Элементы, которые заданы правильно

| Элемент | Ваши параметры | Вердикт |
|---------|---------------|---------|
| **MSUB (SUB1)** | Er=4.6, H=1.5 мм, T=0.035 мм, Rho=1, Tand=0.02 | **Правильно.** Стандартный FR-4, всё корректно |
| **RES R1 (R_drv_A)** | R=50 Ом, последовательно с линией A | **Правильно.** Выходное сопротивление драйвера |
| **RES R2 (R_drv_B)** | R=50 Ом, последовательно с линией B | **Правильно.** Симметрично R1 |
| **RES R3 (R_term)** | R=120 Ом, между выходами A и B | **Правильно.** Терминирующий резистор RS-485, подключён дифференциально |
| **RES R4 (R_rcv_A)** | R=12000 Ом, от выхода A к земле | **Правильно.** Входное сопротивление приёмника |
| **RES R5 (R_rcv_B)** | R=12000 Ом, от выхода B к земле | **Правильно.** Симметрично R4 |
| **CAP C1 (C_rcv_A)** | C=1.5e-05 мкФ = 15 пФ | **Правильно.** Входная ёмкость приёмника |
| **CAP C2 (C_rcv_B)** | C=1.5e-05 мкФ = 15 пФ | **Правильно.** Симметрично C1 |
| **STACKUP (SUB2)** | 3D-визуализация видна | **Правильно.** Нужен для EXTRACT/AXIEM |

### Выбор MCLIN вместо CLIN — хорошее решение

Вы использовали **MCLIN** (Coupled Microstrip Line) вместо CLIN. Это более продвинутый подход:

- **MCLIN** привязан к реальной геометрии (W, S, L) и подложке (MSUB), поэтому импедансы **рассчитываются автоматически** из физических размеров.
- **CLIN** — абстрактные связанные линии с ручным заданием Z0E/Z0O.
- **MCLIN + EXTRACT** позволяет автоматически создать EM-структуру для AXIEM — это правильный путь к визуализации полей.

---

## Ошибка 1 (КРИТИЧЕСКАЯ): PORT3 и PORT4 — Z = 1 000 000 Ом

### Что у вас

```
PORT P=3, Z=1000000 Ohm
PORT P=4, Z=1000000 Ohm
```

### Почему это грубая ошибка

S-параметры нормируются к импедансу порта. При Z = 1 МОм:

```
S[3,1] содержит множитель √(Z₁/Z₃) = √(120/1000000) ≈ 0.011 → −39 дБ
```

Это значит, что **DB(S[3,1]) будет показывать −70...−120 дБ** — выглядит как будто «сигнал не проходит», но на самом деле это **артефакт нормировки**, а не реальные потери.

### Зачем вообще PORT3 и PORT4 в этой схеме?

Посмотрите на вашу схему — на выходной стороне MCLIN уже стоят:
- R3 = 120 Ом (терминатор) **между** линиями A и B
- R4 = 12 кОм + C1 = 15 пФ (приёмник A) к земле
- R5 = 12 кОм + C2 = 15 пФ (приёмник B) к земле

Эти элементы **полностью определяют нагрузку**. PORT3 и PORT4 с Z=1 МОм добавлены как «пробники» для наблюдения сигнала, но в S-параметрическом анализе AWR порты — **не пробники**, а точки нормировки. Высокоимпедансный порт ломает весь расчёт.

### Как исправить: два варианта

**Вариант A (рекомендуемый): удалить PORT3 и PORT4**

Уберите PORT3 и PORT4. Модель станет **2-портовой** (только PORT1 и PORT2 на входе). На выходе — пассивная нагрузка (R3 + R4/C1 + R5/C2). Это корректная полная модель.

Доступные измерения:
- S[1,1] — отражение от входа A (с учётом всей нагрузки)
- S[2,2] — отражение от входа B
- S[2,1] — связь между портами через MCLIN

**Вариант B: изменить Z порта PORT3 и PORT4**

Если хотите видеть S[3,1] и S[4,2], измените импеданс:
- PORT3: **Z = 60 Ом** (= Z0O, импеданс нечётной моды)
- PORT4: **Z = 60 Ом**

Но тогда **уберите R3, R4, C1, R5, C2** — порты с правильным импедансом сами выступают как согласованная нагрузка (Zdiff = 2×60 = 120 Ом).

**Нельзя одновременно** иметь PORT3/4 с Z=60 Ом **и** терминатор R3=120 Ом — это двойная нагрузка.

---

## Ошибка 2 (КРИТИЧЕСКАЯ): L = 10 000 мм (10 метров!)

### Что у вас

```
MCLIN: W=0.4 мм, S=0.3 мм, L=10000 мм
```

### Почему это неправильно

**MCLIN — это модель микрополосковой дорожки на печатной плате.** Длина 10 метров для дорожки на плате — **физически абсурдна**. Типичные длины дорожек RS-485 на плате: 10–50 мм (от микросхемы до разъёма).

Скорее всего, вы хотели задать длину **кабеля** витой пары (10 м — разумная длина для RS-485). Но MCLIN — не кабель! MCLIN рассчитывает импеданс и потери по формулам для **плоских дорожек на FR-4**, а не для витой пары.

Последствия ошибки:
- EM-извлечение (EXTRACT с AXIEM) для 10-метровой структуры — **невозможно вычислить** (нужна память и время, превышающие разумные пределы).
- Даже схемный анализ при L=10 м будет крайне медленным и может дать артефакты.
- Потери в 10 метрах FR-4 (Tand=0.02) огромны — это не реальная ситуация.

### Как исправить: два подхода

**Подход A: моделируем только участок платы**

Замените L = 10000 мм на **L = 30 мм** (30 мм = типичная длина дорожки RS-485 на плате).

Это физически корректная модель участка платы от микросхемы-драйвера до разъёма.

**Подход B: моделируем и плату, и кабель**

Если нужна полная модель «плата + кабель + плата»:

```
PORT1 ── R1(50Ω) ──┐                    ┌── CLIN(кабель) ──┐              ┌── R_term ── ...
                     │  MCLIN (30 мм)    │                   │  MCLIN (30мм) │
PORT2 ── R2(50Ω) ──┘                    └──────────────────┘              └── R_rcv ── ...
                     (дорожка TX)          (витая пара 10м)   (дорожка RX)
```

Здесь:
- **MCLIN** (L=30 мм) — дорожки на плате (с MSUB)
- **CLIN** (Z0E=90, Z0O=60, EL=60°, F0=0.005) — кабель витой пары (без MSUB)
- Два разных элемента для двух разных физических объектов

---

## Ошибка 3 (ВАЖНАЯ): PORT1 и PORT2 — Z = 120 Ом

### Что у вас

```
PORT P=1, Z=120 Ohm
PORT P=2, Z=120 Ohm
```

Плюс R1 = R2 = 50 Ом последовательно.

### Проблема

Импеданс порта задаёт **нормировочный импеданс** для S-параметров. Z = 120 Ом — это необычный выбор. Стандартная практика:

- **Z = 50 Ом** — стандартный измерительный импеданс (порт имитирует 50-Ом генератор/анализатор).
- R_drv = 50 Ом — выходное сопротивление драйвера.
- Суммарное дифференциальное сопротивление источника: 2 × (50 + 50) = 200 Ом.

При Z = 120 Ом:
- Суммарное дифференциальное сопротивление: 2 × (120 + 50) = 340 Ом.
- Это **сильно рассогласовано** с линией (Zdiff ≈ 120 Ом).
- S[1,1] будет показывать большие отражения, не из-за реальной проблемы, а из-за неправильной нормировки.

### Как исправить

```
PORT1: Z = 50 Ом (стандарт)
PORT2: Z = 50 Ом (стандарт)
```

Тогда полная модель драйвера: 50 Ом (PORT) + 50 Ом (R_drv) = 100 Ом на линию → дифференциально 200 Ом. Это даст S[1,1] ≈ −5...−10 дБ из-за рассогласования с линией — это нормально и физически корректно.

---

## Ошибка 4 (ВАЖНАЯ): EXTRACT — X_Cell_Size = Y_Cell_Size = 1 мм

### Что у вас

```
EXTRACT: Simulator=AXIEM, X_Cell_Size=1 мм, Y_Cell_Size=1 мм
```

### Проблема

Размер ячейки сетки **1 мм** при ширине дорожки **W = 0.4 мм** и зазоре **S = 0.3 мм** — это **слишком грубо**. Ячейка сетки больше, чем моделируемые объекты! AXIEM не сможет корректно дискретизировать дорожки.

**Правило:** размер ячейки должен быть **≤ W/3 и ≤ S/3**.

### Как исправить

```
X_Cell_Size = 0.1 мм  (= W/4 = S/3)
Y_Cell_Size = 0.1 мм
```

Это даст ~3–4 ячейки поперёк каждой дорожки и ~3 ячейки в зазоре — минимально достаточно для корректного расчёта.

При L = 30 мм (после исправления длины) это будет 300 ячеек по длине × ~10 по ширине = ~3000 ячеек — расчёт займёт секунды. При L = 10 000 мм — 100 000 ячеек по длине, расчёт невозможен.

---

## Параметры MCLIN: W и S — приемлемо, но проверьте

### Что у вас

```
MCLIN: W=0.4 мм, S=0.3 мм
```

### Оценка

Эти значения отличаются от рекомендованных в гайде (W=0.3, S=0.2), но допустимы. Проверьте через **Tools → TXLine**:

1. Откройте TXLine → Coupled Microstrip.
2. Введите: Er=4.6, H=1.5 мм, T=0.035 мм, W=0.4 мм, S=0.3 мм.
3. Нажмите **Analyze**.
4. Посмотрите Zdiff.

Ожидание: Zdiff ≈ 100–150 Ом. Если далеко от 120 Ом — скорректируйте W и S.

При W=0.4 и S=0.3 Zdiff будет **несколько ниже** 120 Ом (более широкая дорожка → ниже импеданс), возможно ~90–110 Ом. Это приемлемо для учебной модели, но стоит проверить.

---

## Итоговая таблица исправлений

| № | Элемент | Сейчас | Нужно | Приоритет |
|---|---------|--------|-------|-----------|
| 1 | **PORT3 Z** | 1 000 000 Ом | **Удалить PORT3** (или Z=60 Ом без нагрузки) | КРИТИЧЕСКИЙ |
| 2 | **PORT4 Z** | 1 000 000 Ом | **Удалить PORT4** (или Z=60 Ом без нагрузки) | КРИТИЧЕСКИЙ |
| 3 | **MCLIN L** | 10 000 мм | **30 мм** | КРИТИЧЕСКИЙ |
| 4 | **PORT1 Z** | 120 Ом | **50 Ом** | ВАЖНЫЙ |
| 5 | **PORT2 Z** | 120 Ом | **50 Ом** | ВАЖНЫЙ |
| 6 | **EXTRACT X_Cell_Size** | 1 мм | **0.1 мм** | ВАЖНЫЙ |
| 7 | **EXTRACT Y_Cell_Size** | 1 мм | **0.1 мм** | ВАЖНЫЙ |

---

## Как должна выглядеть исправленная схема

### Вариант A: 2-портовая модель (рекомендуемый)

```
PORT1 (Z=50) ── R1(50Ω) ── Pin1 ──┐                 ┌── Pin3 ─┬─ R4(12kΩ) ── GND
                                    │   MCLIN         │         ├─ C1(15пФ) ── GND
                                    │   W=0.4 mm      │    R3(120Ω)
                                    │   S=0.3 mm      │         │
PORT2 (Z=50) ── R2(50Ω) ── Pin2 ──┘   L=30 mm       └── Pin4 ─┼─ R5(12kΩ) ── GND
                                    │   MSUB=SUB1      │        └─ C2(15пФ) ── GND
                                   GND                GND
```

**Нет PORT3 и PORT4.** Измеряем S[1,1] и S[2,1].

### Вариант B: 4-портовая модель (без пассивной нагрузки)

```
PORT1 (Z=50) ── R1(50Ω) ── Pin1 ──┐                 ┌── Pin3 ── PORT3 (Z=60)
                                    │   MCLIN         │
                                    │   W=0.4 mm      │
                                    │   S=0.3 mm      │
PORT2 (Z=50) ── R2(50Ω) ── Pin2 ──┘   L=30 mm       └── Pin4 ── PORT4 (Z=60)
                                    │   MSUB=SUB1      │
                                   GND                GND
```

**Нет R3, R4, R5, C1, C2.** Порты Z=60 Ом сами создают нагрузку Zdiff=120 Ом. Измеряем S[3,1], S[1,1], S[3,2].

---

## Пошаговый план исправлений

### Шаг 1: Исправить MCLIN

1. Дважды щёлкните по MCLIN (ID=TL1).
2. Измените **L** с `10000` на `30` (мм).
3. OK.

### Шаг 2: Исправить PORT1 и PORT2

1. Дважды щёлкните по PORT1 → **Z**: `50` (Ом).
2. Дважды щёлкните по PORT2 → **Z**: `50` (Ом).

### Шаг 3: Решить с PORT3 и PORT4

**Если выбрали вариант A (2-портовая):**
1. Выделите PORT3 → Delete.
2. Выделите PORT4 → Delete.
3. Убедитесь, что все провода подключены к R3/R4/C1/R5/C2.

**Если выбрали вариант B (4-портовая):**
1. Дважды щёлкните PORT3 → **Z**: `60`.
2. Дважды щёлкните PORT4 → **Z**: `60`.
3. Удалите R3, R4, R5, C1, C2.

### Шаг 4: Исправить EXTRACT

1. Дважды щёлкните по блоку EXTRACT.
2. **X_Cell_Size**: `0.1` мм.
3. **Y_Cell_Size**: `0.1` мм.

### Шаг 5: Запуск и проверка

1. Нажмите **F8**.
2. Проверьте:

| Вариант | Параметр | Ожидание |
|---------|----------|----------|
| A (2-порта) | DB(S[1,1]) | < −5 дБ (рассогласование из-за R_drv, но не катастрофическое) |
| A (2-порта) | DB(S[2,1]) | Связь между портами |
| B (4-порта) | DB(S[3,1]) | ≈ −3...−8 дБ (потери из-за R_drv и рассогласования) |
| B (4-порта) | DB(S[1,1]) | −5...−15 дБ |

**Если DB(S[3,1]) = −70 дБ или хуже — PORT3/4 всё ещё имеют Z=1 МОм. Проверьте.**

---

## EXTRACT и AXIEM: правильная настройка для E-field

Вы добавили EXTRACT-блок с AXIEM — это правильный подход для получения S-параметров с учётом реальной электромагнитной модели. Однако:

### AXIEM не визуализирует E-field в объёме

AXIEM покажет:
- S-параметры (точные, с учётом геометрии)
- Current Density (распределение тока на проводниках)

Для визуализации E-field в пространстве нужен **Analyst** (3D FEM). Если Analyst доступен по лицензии:

1. В EXTRACT измените **Simulator** с `AXIEM` на `Analyst`.
2. Или создайте отдельную EM Structure вручную (как описано в основном гайде, раздел 3.2).

Если Analyst недоступен — используйте AXIEM для S-параметров и Current Density, а распределение E-field опишите аналитически (см. Приложение А основного гайда).

---

## Что вы сделали хорошо

1. **MSUB задан корректно** — все параметры FR-4 правильные.
2. **Терминатор R3=120 Ом** подключён правильно — между линиями A и B, а не к земле.
3. **Приёмник (R4/C1 и R5/C2)** — правильная структура: R параллельно C, каждый к земле.
4. **R_drv (R1, R2 = 50 Ом)** — правильное выходное сопротивление драйвера.
5. **EXTRACT с AXIEM** — правильный подход к EM-извлечению.
6. **STACKUP** создан — основа для EM-анализа.
7. **Общая топология схемы** — логически верная.

Исправьте 4 ошибки (PORT Z, длина L, EXTRACT cell size) — и модель заработает.
